schema = 1

[blueprint]
name = "Vite"
description = "A Vite build served as files over HTTPS, from dist/ - run npm install, then npm run build"
created_at = "2026-09-13T00:00:00Z"

[blueprint.created_on]
os = "any"
version = "0.0.1"

[runtimes]
node = "24"

[site]
kind = "static"
doc_root = "dist"
https = true
domain_pattern = "{project}.test"
