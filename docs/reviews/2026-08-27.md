# Review 2026-08-27 — the whole workspace at `d29e6dc`

**Scope.** 9 crates, 135,808 lines of `.rs` (99,244 code, 36,564 comment, 26,016 integration
test), 129 commits since 2026-08-10, a 1,230-line `.github/workflows/ci.yml`, ~115k words under
`.claude/`. `cargo check --workspace --all-targets` is clean. Nothing was changed by this review.

**Method.** Four independent passes — security (IPC, elevate, download chain, secrets, DNS, panics),
tests/CI (gates, `#[ignore]`, coverage, flakiness), docs-vs-code (RPC surface, roadmap, spec
contradictions), code quality (large files, `cfg`, `anyhow`, async, dependencies) — then every
`file:line` cross-checked.

**One-line verdict.** Unusually high discipline: no `TODO`, no `unwrap()` outside tests in
core/daemon, no `unsafe` outside `mixengine-platform`, layering enforced by a test, roadmap matching
the code at 59/59 RPC methods. The real weaknesses are **one pipe-identity hole on Windows**, **CI
that can be green for the wrong reason**, and **the maintenance cost of the comment density**.

---

## Bugs

Wrong against what the code or its documentation claims. Most severe first.

### R1 — The client never verifies who is serving the named pipe (Windows) `[x]`

- [windows/ipc.rs:188](../../crates/mixengine-platform/src/windows/ipc.rs#L188) `connect()` opens
  `\\.\pipe\mixengine.<sid>.<fnv(run)>` and checks nothing about the serving side. The name is
  predictable (`fingerprint()` is FNV-1a; the comment at `:81-91` itself says "a name, not a
  defence"); the Win32 pipe namespace is flat and any account can create in it.
- Consequence: another account on the same machine creates the pipe before the daemon comes up and
  receives everything `mix` sends (every request, `elevation.*` included).
- The daemon half is right: `FILE_FLAG_FIRST_PIPE_INSTANCE` (`:107`, `:257`) makes `bind` *refuse*
  a taken name; DACL `D:P(A;;GA;;;<owner>)` (`:418`); `reject_remote_clients` (`:280`); peer check
  by impersonation (`:334-375`). Only the reverse direction is missing.
- Unix is not exposed: the socket sits in a `run/` already locked down by `DirectoryAccess`, plus
  `0600` ([unix/ipc.rs:112](../../crates/mixengine-platform/src/unix/ipc.rs#L112)).
- Fix: after `CreateFile`, `GetNamedPipeServerProcessId` → open the process → compare its owner SID
  with our own; on mismatch close and return a named error. A private namespace
  (`CreatePrivateNamespace`) is the heavier alternative and not needed for a single-user model.
- This sits inside the "trusted single-user machine" model that
  [security-model.md](../architecture/security-model.md) assumes, but it is the one place where
  that assumption is load-bearing and unstated.
- **Fixed in #41.** `connect` now reads the **owner of the pipe object** and hangs up before the
  first byte on one this account does not own, with `Error::EndpointNotOurs` naming who answered.
  Not the pid
  the fix above suggested: `GetNamedPipeServerProcessId` hands back a number the OS may have reused
  by the time it is turned back into a process, which is the very objection `peer_of` states for the
  other direction — while an owner is stamped on at creation and cannot be set to an account the
  creator does not hold. That made a second change necessary: the pipe's descriptor now *states*
  `O:<sid>` instead of letting the creating token supply a default owner, which is a machine policy
  ("Default owner for objects created by members of the Administrators group") and not a fact.
  `bind`'s probe was widened to the same question, because it is the message a squat surfaces
  through *first*: it already dialled the name to tell "taken" from "refused", and answering "another
  process is already listening" sends the user hunting for a daemon of their own to stop while a
  stranger holds the name. It now says who, through the same `EndpointNotOurs`.
  Autostart needed nothing — `is_absent` in the CLI already excluded everything but
  `NotFound`/`ConnectionRefused`, so the new error is reported rather than answered by starting a
  second daemon. Both error mappers did: the variant fell through to `internal` in each, which tells
  the person holding the one message that says another account is on their endpoint to go and file a
  bug. Both now answer `conflict`, and neither hint offers `mix daemon stop` — there is no daemon of
  theirs to stop.
  Seven unit tests in `windows/ipc.rs` and one in each mapper: the descriptor names an owner (this
  one fails without the `O:`), the pipe a `Listener` creates is owned by this account, dialling it as
  its owner works, dialling it as somebody else is refused, and the three answers `bind`'s own probe
  can give are told apart. The account boundary itself is what none of them cross — Windows will not
  let a standard process claim another account's SID as an owner, so what the refusal tests vary is
  the *expectation*, everything else being a real pipe and a real read off it. The genuine
  cross-account case needs a second account and belongs to the `system` job, which R3 shows does not
  run yet.
  The assumption is now written down, in [security-model.md](../architecture/security-model.md) and
  in [daemon-and-ipc.md](../architecture/daemon-and-ipc.md).

### R2 — The `connections` suite says it "runs in the system job", and the job never calls it `[x]`

- [connections.rs:26](../../crates/mixengine-platform/tests/connections.rs#L26):
  `#[ignore = "opens a real socket; runs in the system job"]`.
- `grep -- '--test connections' .github/workflows/ci.yml` → nothing. The `system` job
  (`ci.yml:845`) names each suite explicitly; this one is not in the list.
- Fix: add it to all three legs, or correct the ignore reason.
- **Fixed in #42**, by adding the step rather than by correcting the reason: a suite that measures
  the one thing about this capability no unit test can reach is worth a runner. **The `test` job and
  not `system`**, though, which is what the suite claimed for as long as nothing ran it. Counting
  connections needs no administrative token, and `system` is the job for what an unprivileged
  process cannot prove — what this needs is a real socket on each of the three mechanisms, and
  `test` is the job with a leg on each. It is `#[ignore]`d and given a step of its
  own in the `certutil` step's shape rather than being let into the workspace run, so a leg where it
  did not run says so instead of reporting a pass. On Linux it sits outside the network namespace,
  deliberately: inside it the tables hold this test's own sockets and nothing else, while picking one
  port out of the machine's whole table is the harder question and the one the idle sweeper asks.
- **The first real measurement is green on all three** — run 33169664473, one test run and passed on
  each of `windows-latest`, `ubuntu-latest` and `macos-latest`. Measured on this machine and in WSL
  before it was pushed, so the round trip was asked for macOS's answer, which is the one nothing here
  can give.
- **One thing macOS's green leaves open**, and it is not this finding's to close: `lsof -iTCP:<port>`
  matches the port at *either* end, so both sockets of a loopback connection are files it can see —
  yet `-t` answered with one line. Either the address filter matched only the local end, or `-t`
  prints one line per *process* and not per file, which would make `established_on` a count of
  processes and the note at
  [macos/ports.rs:49](../../crates/mixengine-platform/src/macos/ports.rs#L49) ("Two clients from one
  process are two lines") wrong. **Nothing in the build can tell today**: the sole consumer,
  [idle.rs:85](../../crates/mixengine-supervisor/src/idle.rs#L85), maps `Ok(0)` to `Idle` and every
  other count to `Busy`, so the two readings are the same answer. Worth a finding of its own the day
  a caller wants the number itself.
- **The audit this finding invites came back with one name.** Every other `#[ignore]`d suite no
  `--test` line mentions is either conditional on `not(debug_assertions)` — the `fakeservice` ones,
  which do run, in the debug build CI uses — or is R3's and `index.rs`'s. `connections` was the only
  suite claiming a job that never called it.

### R3 — `MIXENGINE_SYSTEM_TESTS=1` is mentioned in four places and set in none `[x]`

- [cert.rs:428](../../crates/mixengine-cli/tests/cert.rs#L428) and
  [caddy.rs:303](../../crates/mixengine-cli/tests/caddy.rs#L303) are double-gated: `#[ignore]`
  **and** `std::env::var("MIXENGINE_SYSTEM_TESTS") == "1"`. No file under `.github/` exports the
  variable.
- Consequence: **CA rotation and trust-store writes have never run in CI**, although T52–T54 are
  ticked `[x]`.
- Fix: export it in the `system` job (that is the job designed to write to the machine) and add
  the two suites to its list.
- **Fixed in #43.** The `system` job sets the variable and runs both suites, but **not on the
  same three legs**, which is where this diverges from the fix above. The `cert` invariant runs
  everywhere; the end-to-end `caddy` rotation runs on Windows and macOS only.
  It asserts `outcome == "rotated"`, so it needs a grant — and a Linux runner has no polkit agent,
  which is not a gap in the job but ADR 0005's worst branch, asserted for real by the launcher suite
  beside it. On Linux the rotation is refused whoever asks, root included, so that test could only
  ever fail there; what Linux answers is the refusal, and that is exactly what `cert` asserts. macOS
  earns its leg from a measurement already in this job: the launcher step established that `do shell
  script … with administrator privileges` on a process that is already root runs straight through,
  so the suite runs under `sudo` and the rotation is granted. The job also had to learn to build the
  CLI, the daemon and the helper into one directory, fetch a real Caddy, and it went from 20 minutes
  to 35 with clocks of its own on the steps that could hang rather than fail.
- **The first run of it found a bug three tasks had been carrying, and that is the whole argument for
  this finding.** T51 renders a certificate fingerprint into the generated site file so that a
  reissue produces bytes that *differ* — which is what makes `document::install` notice and ask the
  front end to re-read. That half works. The other half does not: `caddy reload` adapts the Caddyfile
  to JSON and **the adapter throws comments away**, so the configuration Caddy is handed after a
  reissue is identical to the one it is running, its admin endpoint skips the load, and the process
  goes on presenting the certificate it holds in memory. Both legs found precisely that — a rotation
  reporting success, the leaf on disk signed by the new authority, the handshake presenting one
  signed by the old, `problem: served_certificate_differs`, `trust: rejected`. The line that exists
  to trigger the reload is the one line the server never sees. `recipes::caddy` passes `--force` now,
  which `caddy reload --help` on the pinned 2.11.4 documents as *force config reload, even if it is
  the same* — measured against the binary rather than read about. **T52's renewal reissues on the
  same path**, so a renewed certificate was never served either; nothing had ever reissued against a
  running Caddy and read the handshake back.
- **Two things this left behind rather than closed.** `ca-rotate` reports success when the front end
  has been *told*: the registry writes the rendering and notifies, the service's own task reloads. So
  the acceptance criterion is bounded rather than instant, and the test polls for it — which is what
  "afterwards" means and not a softening of it, but it does mean `mix cert status` run in that window
  answers `served_certificate_differs` about a rotation that worked. And
  [document.rs:308](../../crates/mixengine-core/src/generate/document.rs#L308) stages every install
  into **one fixed directory per service** (`.caddy.staging`), which `stage` opens by deleting and
  recreating, with no lock anywhere in [generate.rs](../../crates/mixengine-core/src/generate.rs)
  around `declared`. Two renders of one service that overlap therefore corrupt each other, and the
  symptom is exactly the one a Windows leg produced once on this branch: `stage` reports success and
  the validator is then handed a directory with no entry file in it. It did not reproduce on a rerun
  and is not fixed here; the defect is provable from the code whether or not that run is what it was.

### R4 — `test-no-network.sh` downgrades a missing package to `::warning`; the job stays green `[x]`

- [test-no-network.sh:143](../../.github/scripts/test-no-network.sh#L143), `:152`, `:160`, `:191`,
  `:201`, `:211`, `:219`, `:225` — when `MIXENGINE_<X>_PACKAGE` is unset, that recipe's real-server
  suite is skipped with a warning.
- Consequence: a broken package fetch (mirror down, index rotated, checksum mismatch) removes every
  Caddy/nginx/MariaDB/MySQL/PostgreSQL/Redis/Memcached test from the Linux leg and the result is
  still ✅.
- Fix: `exit 1` with the same message. If a developer machine without packages needs an escape
  hatch, gate it behind its own variable (`MIXENGINE_ALLOW_MISSING_PACKAGES=1`), not the default.
- **Fixed in #44**, with the escape hatch this asked for: ten `::warning` branches now call one
  `missing` helper that prints `::error` and exits 1, and only warns when
  `MIXENGINE_ALLOW_MISSING_PACKAGES=1` is set — a variable somebody has to type, which is the
  difference between an exception and a default. It is written down in
  [build-and-release.md](../operations/build-and-release.md) beside the other knobs, and nothing in
  CI sets it.
- **The consequence as stated does not hold today, and saying so is part of the record.** A mirror
  that is down does not reach these branches at all: every fetch step runs `set -euo pipefail` with
  `curl --fail --retry 3`, so a failed download fails *that step* and the job stops before this
  script is reached — and `Linux-X64`, the only architecture this leg runs on, has a published
  archive for all nine packages. The warning branches were unreachable on CI rather than quietly
  firing. What the change buys is therefore not a hole closed but a contract enforced by the thing
  that holds it, for two live cases: a fetch step that learns to skip on Linux — **three of them
  already do exactly that on Windows-ARM**, `::notice` and `exit 0` — and a developer running the
  script by hand, which is who the warning was really written for.
- **One thing of the same shape is deliberately left alone.** `ci.yml`'s nginx step carries
  `if: … && env.MIXENGINE_NGINX_PACKAGE != ''`, which is a step that removes itself when a variable
  is empty — this finding's shape, in the workflow rather than in the script. It has a reason
  written beside it and it serves a Windows-on-ARM leg that does not exist yet, where no upstream
  publishes an nginx; taking the guard away would break that leg on the day it is added, to close a
  path nothing walks. The day an ARM leg lands is the day to decide whether a leg that cannot run a
  suite should say so as a skip or as a smaller matrix.
- A stale comment beside the change went with it: "the one `#[ignore]`d suite this job runs" had
  nine more below it.

### R5 — `std::fs::remove_dir_all` inside an `async fn` `[x]`

- [elevation.rs:595](../../crates/mixengine-daemon/src/elevation.rs#L595), inside `flush()`
  starting at `:535`. The **only** place in daemon+core that does synchronous I/O on a tokio
  worker; everything else goes through a documented `spawn_blocking` (`api/logs.rs:159`,
  `rpc.rs:662`, `certs.rs:384/420/666`, `runner.rs:1621/1760/2009`, …).
- Low impact (a small request directory, best-effort), but it is the one exception to a rule the
  rest keeps absolutely.
- Fix: `tokio::fs::remove_dir_all(...).await`.
- **Fixed in #45**, as written, and through `tokio::fs` rather than the explicit `spawn_blocking` the
  certificate and log paths use: there is one call to move, and `tokio::fs` *is* that hand-off,
  without a closure for the next reader to look past.
- **"The only place" was checked rather than taken.** Fixing one instance of an invariant and
  leaving its siblings is the failure this workspace's own working agreements name, so the sweep was
  run: `diagnostics.rs` holds six synchronous calls and every one of them is inside `pack`, a
  non-async fn reached through the `spawn_blocking` at
  [diagnostics.rs:163](../../crates/mixengine-daemon/src/diagnostics.rs#L163); every `std::fs` in
  `mixengine-core` sits in a synchronous function — `ca::discard`, `leaf::read`, `config::read`,
  `elevation::write_request` — because core is a synchronous library and the daemon is where the
  hand-off lives; the rest is `#[cfg(test)]`. The finding was right, and now it is known to be.
- **No test distinguishes this change, and inventing one would be theatre.** It alters *how* and not
  *what*: the assertion that the single-use directory is gone after a grant
  ([elevation.rs:1033](../../crates/mixengine-daemon/src/elevation.rs#L1033)) passes before and
  after, which is exactly what a change of this kind must leave true.

### R6 — `security-model.md` describes an authentication mechanism that does not exist `[x]`

- [security-model.md:136-137](../architecture/security-model.md#L136-L137): an optional TCP
  listener requiring `Authorization: Bearer` from `run/api.token`, regenerated on every start; plus
  scoped extension tokens.
- `grep -rn 'TcpListener\|Bearer\|api\.token' crates/mixengine-daemon/src` → only test scaffolding
  in `certs/handshake.rs`. No listener means no vulnerability — but a security document describing
  a control that is not there is how a later reviewer concludes that authentication exists.
- Fix: mark the section "**not built** — arrives with T80 (extensions)"; or move it to
  [extensions.md](../features/extensions.md), where it belongs.
- **Fixed on `master`**, directly: a markdown-only change takes no branch, no PR and no CI run in
  this repository.
- **Two files and not one.** [daemon-and-ipc.md](../architecture/daemon-and-ipc.md) said the same
  thing in the same present tense — "`--listen 127.0.0.1:PORT` exists for debugging and for
  remote-container setups; when enabled it requires a bearer token" — and marking one document while
  its neighbour goes on promising the control would leave the finding exactly where it was. They
  share an invariant, not a file.
- **The decision was already written down; what was missing was the two documents agreeing with
  it.** [T8](../roadmap/phase-0-foundations.md) records the listener as deliberately not built —
  *"a second transport and a second access-control story for a case nobody has yet"* — so neither
  document needed a new judgement, only a pointer to the one that exists. Both now say **not built**
  and quote the reason, and `security-model.md` opens the section by saying which of its three
  bullets is real, since that is the question a reader of a security document is actually asking.
- The extension bullet is marked **not built — arrives with T80** and reduced to a pointer at
  [extensions.md](../features/extensions.md), which already carries the rule. A second copy of a
  permission model is how the two come to disagree.

### R7 — The docs say Caddy is the default; the code has no default `[x]`

- [ADR 0004](../decisions/0004-caddy-as-default-web-server.md) and
  [services.md:10](../features/services.md#L10) say Caddy is the default web server.
- `services/front_end.rs` only refuses a **second** front end by `Role::FrontEnd`; nothing installs
  or picks Caddy when there is none. The user has to `mix package install caddy|nginx` themselves.
- Harmless (T37 notes it), but the clearest spec–code drift found. One sentence in `services.md`
  fixes it: "default" means *proposed at first-run setup (T85)*, not *installed by itself*.
- **Fixed on `master`**, directly, as a markdown-only change — one paragraph under the catalogue in
  [services.md](../features/services.md) saying what "default" means: which one this project picks
  when there is a choice, not one that arrives by itself.
- **ADR 0004 is left exactly as it is**, and that is the point rather than an omission. It decides
  *which* front end the role should be, and that decision is correct and unchanged; what drifted was
  a table cell read as a promise about installation. Editing an accepted ADR to fix a misreading of
  it elsewhere is what [the working agreements](../../CLAUDE.md) forbid, and it would also be the
  wrong file.
- **Two parentheticals in the finding did not survive checking, and the fix does not repeat them.**
  "Proposed at first-run setup (T85)" — T85 is *Installers*; no task anywhere offers to install a
  front end on a first run, and the nearest thing in the roadmap is T47's PATH entry. And "T37 notes
  it" — T37's note is about *one* front end being a rule nothing could break until there were two,
  not about nobody installing the first. So the paragraph says the true thing, which is that the
  first run is not built and has no task of its own; pointing at T85 would have replaced one wrong
  promise with another.

---

## Improvements

Correct, but expensive, fragile, or off the standard the rest of the workspace keeps.

### R8 — `anyhow` leaks into the daemon's internal data types `[x]`

- [services/mod.rs:397](../../crates/mixengine-daemon/src/services/mod.rs#L397)
  `Unavailable(anyhow::Error)` is a variant of the service state enum.
- [runner.rs:142](../../crates/mixengine-daemon/src/services/runner.rs#L142)
  `type Resolved = (…, Vec<(String, anyhow::Error)>)`; `:1945` a trait method returns
  `anyhow::Result`; `services/fixture.rs` an entire trait (6 signatures).
- [error.rs:540](../../crates/mixengine-daemon/src/error.rs#L540) admits it: *"The source is still
  `anyhow`"*.
- The other six crates use `thiserror`, exactly one enum per crate. The daemon is a binary, so
  `anyhow` in `main` is fine; once it sits inside a state enum, a client gets a string instead of an
  error code — against "no business logic in clients".
- Fix: an `enum Unavailable { Keyring(..), Package(..), Recipe(..) }` in `services/`, mapped to
  `ErrorCode` like the rest of `error.rs`.
- **Fixed on `fix/r8-typed-errors-in-the-daemon`, and not with the enum above.** `SpecSource` now
  returns `mixengine_core::Result`, so `Undeclarable::Unavailable` carries a
  `mixengine_core::Error` like its sibling. A new enum in `services/` is what the trait's own
  `# Errors` doc already argued against — *"restating it here would be a second list to keep in
  step"* — and that argument is right: every implementation renders through `mixengine-core`, so a
  daemon-local vocabulary would be a translation of a list nobody disagrees with.
- **The consequence as stated does not hold today.** A client does not get a string:
  [error.rs](../../crates/mixengine-daemon/src/error.rs) already **downcast** the `anyhow::Error`
  back to `mixengine_core::Error` to recover the code, and the downcast always succeeded because
  both `Rendered` methods hand over a bare core error. The defect is one level up — a type that
  discards what the layer above it then has to dig back out — and the danger is that the digging
  works *silently* until somebody adds a `.context(…)` on the way out, at which point a misspelled
  setting is classified `internal` and its author is sent to file a bug report. That is the failure
  the arm's own comment was written to prevent.
- **Three tests failed, and what they were pinning down was the hole rather than the product.** Two
  asserted `internal` for a source failure — reachable only because `fixture::Unavailable` raised a
  bare string with no code to find, which the running daemon never does. The fixture now returns
  `NotFound { kind: "package" }`, the same variant `core::packages` uses for a package that is not
  installed, so the answer is `not_found` with the hint `mix package list` instead of `internal`
  with "file a bug report". The tests were renamed and their reasoning rewritten to say what they
  now prove, rather than having a number quietly changed to make them green.
- **Half of the finding is deliberately not fixed, and the code says so where it lives.**
  `Resolved` in [runner.rs](../../crates/mixengine-daemon/src/services/runner.rs) keeps its
  `anyhow::Error`, because that error's whole journey is `tracing::error!` and then
  `StateReason::SpawnFailed` — nothing downcasts it and nothing maps it to a code, so the "a client
  gets a string" argument does not reach it. Three unrelated things produce one, and the
  `.context("the environment entry …")` naming the variable is the part a reader needs; a typed
  enum there would be three variants that exist to be `Display`ed.
### R9 — `deny.toml` allows duplicate versions, and the tree is using that allowance `[x]`

- [deny.toml:40](../../deny.toml#L40) `multiple-versions = "warn"`.
- `Cargo.lock` currently holds `windows-sys` ×3 (0.61.2 / 0.60.2 / 0.59.0), `windows-targets` ×2,
  `syn` ×2, `hashbrown` ×2, `getrandom` ×2 (0.2.17 / 0.4.3), `cpufeatures` ×2.
- The `Cargo.toml` comment calls `getrandom` "the one source of randomness in this workspace" —
  true of the *direct edge*, false of the *tree*. A small contradiction, but the only place found
  where a comment and reality disagree.
- Fix: unify what can be unified (`windows-sys` is mostly pulled by `tokio`/`mio`/`socket2` —
  follow upstream), `skip` the rest by name in `deny.toml` with a reason, then raise the level to
  `"deny"` so a new duplicate is a red build rather than a warning that scrolls past.
- **Fixed** in `chore(deps): make a duplicate version a decision somebody wrote down (R9)`.
  `multiple-versions` is `"deny"`, five `skip` entries and three `skip-tree` roots carry the
  duplicates the tree has today, each naming the edge that causes it, and
  [../standards/rust.md](../standards/rust.md) says where a contributor who trips the new gate
  should go. The gate was watched failing before it was trusted: removing the `syn` entry makes
  `cargo deny check bans` exit 2, restoring it exits 0.
- **The list in this finding was a sample, and one number has already drifted.** `cargo deny` with
  this repository's own six targets reports ten duplicated crates, not six: `windows-sys` is ×4 and
  not ×3 (`ring` pins 0.52.0 as well), and the six `windows_*` import libraries and `windows-targets`
  are duplicates in their own right. They are not separate decisions, though — each is a leaf of a
  `windows-sys` that is already listed — which is why three `skip-tree` roots collapse nine entries.
- **"Unify what can be unified" turned out to be empty, and the parenthetical explaining it is
  wrong.** `tokio`, `mio` and `socket2` are all already on `windows-sys` 0.61.2, the same version
  this workspace takes directly; there is no upstream to follow. Of the ten, exactly one is ours to
  remove — `windows-sys` 0.60.2, under `keyring` 3.6.3 — and `keyring` 4 rebuilt every backend into
  a crate of its own (`windows-native-keyring-store`, `dbus-secret-service-keyring-store`, …), so
  the feature names change and so does the direct edge [ADR 0013](../decisions/0013-reading-the-d-bus-error-name-to-tell-an-absent-store.md)
  takes. That is a migration with a credential store on three OSes behind it, not a version bump,
  and it is left as its own piece of work rather than smuggled into a lint-configuration commit.
- **The comment contradiction is real but not the one stated, and there is a second one.**
  "The one source of randomness in this workspace" is defensible about *our own code*; what was
  actually false is the sentence after it — "already in the tree through `rustls` and `sqlx`".
  Neither reaches `getrandom` at run time. Both reach 0.4 only through `cc`'s `jobserver`, a
  *build* dependency of `aws-lc-sys` and `libsqlite3-sys`. The edges that make the claim true are
  `rand` 0.10 (which is `hickory`) and `tempfile`. The second copy, 0.2, is `ring` and `minisign`,
  and no choice at this edge removes it.
- The second contradiction is 70 lines further down the same file and points the other way:
  `rcgen`'s comment justified its default `ring` backend with "which `rustls` already puts in this
  tree". It does not — `rustls` here runs on `aws-lc-rs`, as `rustls`'s own comment says forty lines
  later, and `ring`'s only parents in the whole graph are `rcgen` and its `x509-parser`. So `rcgen`
  *is* what puts `ring` in: a new package, not a new edge. The comment now says that, and records
  that `rcgen`'s `aws_lc_rs` feature would unify the two backends at the cost of `aws-lc-sys` —
  named as an option, not taken, because which provider signs a certificate is not a manifest
  comment's decision to make.

### R10 — Essay-density comments in the daemon `[-]`

- [services/mod.rs](../../crates/mixengine-daemon/src/services/mod.rs): 893 real code lines, 83%
  comment-to-code, 24 blocks of ≥12 lines (longest 38 lines at `:1331`).
- [runner.rs](../../crates/mixengine-daemon/src/services/runner.rs): 987 code lines, 86%, 29
  blocks (longest 34 lines at `:1821`).
- [rpc.rs:831-884](../../crates/mixengine-daemon/src/api/rpc.rs#L831-L884): a 54-line block for
  `daemon_shutdown()`. `Cargo.toml` and `ci.yml` (1,230 lines) share the style.
- Workspace-wide 26.9% of lines are comments. The density is **concentrated in the hard modules**
  (`render.rs` has 2 blocks, `archive.rs` 1), so it is a choice rather than a habit; and **no
  comment was found to be stale** against the code — very much worth noting.
- The risk is cost, not correctness: at ~8 commits/day, the accompanying prose is the first thing
  that will slip, and a second person has to read twice as many lines to understand a module.
- Suggestion: keep the blocks that *explain a decision* (they stand in for ADRs at function
  scale); move the blocks that *retell history* ("T15a did…", "previously…") to the task's write-up
  in `roadmap/phase-*.md` and leave a one-line link. Estimated 20–30% cut in the two files above
  without losing a single reason.
- **Deliberately not fixed.** The measurement holds and was reproduced — 25 blocks of ≥12 lines in
  `services/mod.rs` against the finding's 24, 30 in `runner.rs` against 29, and the three longest
  are exactly where it says they are (38 lines at `mod.rs:1337`, 34 at `runner.rs:1830`, 54 at
  `rpc.rs:831`). What does not hold is the estimate, because the category it would harvest from is
  nearly empty.
- **The 20–30% was estimated, not counted.** Those 55 long blocks are 1,098 lines between them.
  Seven contain a "used to"/"previously" clause *at all* — `mod.rs:383, 1337, 1552, 2312, 2525,
  3574` and `rpc.rs:2434` — and in every one of the seven the clause is a sentence or two inside a
  block that is otherwise decisions. The whole harvest the stated mechanism can take is of the order
  of fifteen lines out of 1,098: about 1.5%, not 20–30%.
- **The clauses are not retellings.** Each is the failure the current shape exists to prevent, which
  is the strongest form a reason takes. `mod.rs:3574` is a test's doc, and its "it used to walk past
  the place the shutdown had already been…" *is* the specification of what that test catches — the
  test loses its meaning without it. `mod.rs:383` and `rpc.rs:2434` are R8's own, and each warns a
  future reader off putting `anyhow` back and off re-pinning the hole three tests were pinning.
- **The T-numbers are provenance, not history.** 60 of `mod.rs`'s 1,056 comment lines mention a task
  id, and virtually all are a label on a decision line — "roadmap task **T18**" — which is the
  code↔roadmap traceability `CLAUDE.md` asks be kept current. Deleting a label shrinks a block by
  nothing.
- **There is no duplication to collapse either.** Across the whole `mixengine-daemon` crate, nine
  sentences of ≥70 characters appear in more than one place, and most are one-line field docs
  repeated between a struct and its builder in `api/mod.rs`.
- **And the remedy is already the written rule.** [../standards/rust.md](../standards/rust.md),
  "Structure & style": *"A comment earns its place by carrying what the code cannot… one that
  restates a note in `.claude/` is worse, since two tellings of a decision are two places for it to
  drift."* That is this finding's suggestion, already codified — and the finding's own observation
  that no comment was found stale is the evidence it is being followed. Cutting a fifth of the
  reasons out of the two hardest modules on an estimate would spend the thing this workspace is
  deliberately built around to buy a risk that has not yet materialised.
- The cost the finding names is real and is not disputed. What is missing is a defect: it says so
  itself — *"the risk is cost, not correctness"*, and *"no comment was found to be stale"*. Left
  open would mean a standing invitation to delete reasons; marked `[-]` with the count above, a
  later review can re-raise it against evidence rather than against an estimate.

### R11 — Three large files bundle responsibilities that split cleanly `[ ]`

- `Registry` in [services/mod.rs](../../crates/mixengine-daemon/src/services/mod.rs) holds four
  clearly bounded jobs: recovery/reconcile (`:780-1150`), the start/stop walk (`:1152-1290`),
  rituals + front-end lookup (`:556-780`), the supervision map + claim/stop (`:1294-1660`).
- [rpc.rs](../../crates/mixengine-daemon/src/api/rpc.rs): 82 one-method-one-fn handlers;
  splitting by namespace (`service.*`, `site.*`, …) is mechanical.
- [cli/main.rs](../../crates/mixengine-cli/src/main.rs): 26 clap structs + 48 fns, 1,779 real code
  lines, no in-file tests; splitting by subcommand is mechanical too.
- None of them is tangled; this is "big", not "complex". Low priority — do it when touching the
  file for another reason, not as a PR of its own.
- **Left open on purpose, which is what the last bullet asks for.** Checked rather than assumed:
  every count reproduces — `main.rs` still has 26 clap types, 48 functions and no test in the file,
  and `rpc.rs` still has 82 handlers.
- **The line ranges have already drifted and are not worth repairing**, which is itself the argument
  for the last bullet. R8 added ten lines above them, so `:780-1150` and the three beside it now
  land in the middle of a doc comment. The boundaries are real, though, and they are method groups
  rather than line spans — recorded here so the next person to open the file for another reason does
  not have to find them again:
  - **rituals and the front-end lookup** — `front_end_program`, `front_end_tls_port`,
    `remember_rituals`, `ritual_for`, `set_limits`, `stopping_because`, `ask_to_reload`, `hand_over`.
  - **recovery and reconcile** — `recover`, `reconcile_stranded`, `reconcile`, `discard`.
  - **the start/stop walk** — `start`, `stop`, `shut_down`.
  - **the supervision map and the claim** — `is_running`, `begin`, `supervise`, `stop_one`,
    `claim_stop`, `block`.
  Nothing crosses those four groups except the two locks `Registry` holds, which is why the finding
  is right that they split cleanly.

### R12 — Integration coverage is thinnest in the two largest crates `[x]`

| Crate | src | tests/ | ratio | in-src `#[test]` |
| --- | --- | --- | --- | --- |
| mixengine-core | 29,912 | 3,173 | 0.11 | 408 |
| mixengine-proto | 13,492 | 130 | 0.01 | 232 |
| mixengine-daemon | 27,198 | 5,824 | 0.21 | 277 |
| mixengine-cli | 6,739 | 9,135 | 1.36 | 46 |

- Workspace-wide ~0.24. `proto` has 232 unit tests, so its 0.01 is not a worry; `core` is the
  largest crate with the lowest integration ratio — most of its logic is proved indirectly through
  the `cli` and `daemon` suites.
- Suggestion: when changing `core/generate/` or `core/install/`, add the test under `core/tests/`
  rather than only under `cli/tests/`, so a failure points at the right layer.
- **Fixed** in `docs(standards): say which layer a test belongs to (R12)` — the suggestion is now a
  paragraph under "Layers" in [../standards/testing.md](../standards/testing.md), which is where a
  practice in this repository is kept. What it says is the part of this finding that survives
  checking: prove a rule where the rule lives, and let the `cli` suite prove the thing only it can,
  that the command reaches it.
- **The ratios reproduce exactly, and two more make the table read differently.**
  `mixengine-platform` is 0.16 and `mixengine-supervisor` 0.28, so `core`'s 0.11 is the low end of a
  spread rather than an outlier against one comparator.
- **The headline does not follow from the number, by this finding's own reasoning.** It excuses
  `proto`'s 0.01 because "`proto` has 232 unit tests, so its 0.01 is not a worry" — and `core` has
  **409**, the most of any crate here. Per line of source that is one unit test every 73 lines
  against `proto`'s every 58: the same order. `core/generate/` alone carries 174 of them across
  10,517 lines. What the ratio measures is where a crate keeps its tests, and `core` keeps them
  next to the code, which is where the Layers table already says most tests should be.
- **No test was written to move the ratio**, which is the trap this finding sets if it is read as a
  target. A test comes from a behaviour somebody is changing; one written to raise a number proves
  nothing and has to be maintained anyway. R3 is the shape the new paragraph asks for and it already
  happened: the Caddy reload rule got a `#[test]` beside the recipe *and* the `cli` suite kept the
  end-to-end check that the front end really reloads.

### R13 — A few fixed `sleep`s in tests `[x]`

- [lifecycle.rs:288](../../crates/mixengine-daemon/tests/lifecycle.rs#L288) 1000 ms;
  `supervisor/tests/ready.rs:168` 200 ms, `:273` 300 ms; `daemon/tests/idle.rs:207` 250 ms.
- The rest (39 `sleep(` in tests) are 10–25 ms polls inside a deadline loop (`api.rs:91`,
  `supervision.rs:542`, `testkit/home.rs:303-310`) — the right shape. The four above carry a
  comment justifying the margin, but `lifecycle.rs:288` is the likeliest to go red on a busy
  Windows runner (see the note on the slow Windows leg).
- Suggestion: turn them into wait-until-condition with a deadline like the rest; keep the
  millisecond figure as a *ceiling*, not as the *wait*.
- **Fixed** in `test(supervisor): assert the retry a command ready check exists for (R13)` — but for
  one of the four, and not for the reason given. Each of the other three was read before it was
  touched, and none of them is what this finding takes them for.
- **`ready.rs:273` was the real one, and the defect is the opposite of flakiness.** Its own doc says
  *"The point is the retry: the first runs fail… and the check is only satisfied when one of them
  succeeds"* — and nothing asserted that. The stimulus arrived after a fixed 300 ms against a check
  whose retry interval is `COMMAND_RETRY` = 250 ms and whose every attempt is a **process spawn**. On a
  runner where `cmd.exe` costs more than 300 ms the sentinel lands before the first attempt reads
  for it, the check succeeds first time, and the test is green with its whole point missing. It
  could never go red; it could only stop proving anything.
- The fix is not a deadline loop around the wait, because the wait already had a 20 s ceiling. The
  probe is now a script the test writes, it appends a byte, and the stimulus waits on that byte
  instead of on a guess — with `assert!(probe.made() >= 2)` to say out loud what the test is for.
  Watched failing first: with the sentinel written immediately, which is exactly the slow-runner
  case, the new assertion fails with `made() == 1`.
- **The first attempt at the probe had the same bug it was written to fix**, and the suite found it —
  green alone, red under the other seven. Marking the attempt *before* testing the sentinel let the
  test see an attempt that had not yet looked, so the sentinel landed mid-attempt and that attempt
  passed. The order is now test, then mark, then answer, so a byte on disk means an attempt whose
  verdict can no longer change. Twelve consecutive green runs of the full suite.
- **`ready.rs:168` is a stimulus, not a wait, and its margin is sound.** The 200 ms sits inside a
  spawned task that binds the port; the wait is `ready::wait` with a 20 s ceiling. A refused
  connection on loopback returns in microseconds against a 50 ms retry, so the check gets about four
  attempts before the port opens — where the command check got barely one. That asymmetry is now
  written at both sites, since it is the only thing that makes one of them safe and the other not.
- **`lifecycle.rs:288` cannot be a deadline loop and is not flaky in the direction claimed.** What it
  asserts is that *nothing happened* for a second — a negative, with no condition to poll. And the
  only way it fails is the parent giving up **early**, while a busy Windows runner delays every poll
  the parent makes: slowness adds evidence here rather than removing it. The finding names it "the
  likeliest to go red on a busy Windows runner", which is backwards. The reasoning is recorded at the
  line so the next reader does not have to redo it. The other wait in that same test was already
  converted to `wait_until_daemon_log_says`, which is this finding's own prescription applied where
  it applies.
- **`idle.rs:207` is already the shape the finding asks for.** It is the poll interval inside
  `wait_for`, a `while Instant::now() < deadline` loop bounded by `PATIENCE` that panics with the
  daemon log. It belongs with the 39 the finding praises as "the right shape"; it is 250 ms rather
  than 10–25 ms only because that test waits a minute. Left alone.

### R14 — "No `#[cfg]` outside platform" is kept by hand only `[x]`

- [workspace_layering.rs](../../crates/mixengine-proto/tests/workspace_layering.rs) enforces
  dependency direction and testkit-as-dev-only — good. No test forbids
  `#[cfg(windows|unix|target_os)]` in the `src/` of core/daemon/cli/supervisor.
- Currently clean: exactly 5 `cfg`s in core+daemon, all after `#[cfg(test)]`
  (`recipes/postgres.rs:797/817/845` after `:581`; `install/archive.rs:251/253` after `:214`).
  But it is "clean by discipline", not "clean by gate".
- Suggestion: a test in the same `workspace_layering.rs` that scans `src/**/*.rs` of every crate
  other than platform/elevate/testkit and fails on `#[cfg(windows` / `#[cfg(unix` / `target_os`
  outside a `#[cfg(test)]` region.
- **Fixed** in `test(proto): gate the rule that per-OS code lives in platform (R14)`, as
  `no_crate_but_platform_compiles_a_line_away_by_operating_system`, beside the layering test as
  suggested. Watched failing three ways before it was trusted: an OS `#[cfg]` in a file with no
  permission, one in a permitted file but above its test module, and a permitted file grown a second
  `#[cfg(test)]`. Each names the file and the line and says what to do instead.
- **"Outside a `#[cfg(test)]` region" could not be implemented as written**, and finding out why
  changed the shape of the gate. `#[cfg(test)]` in this workspace is not one `mod tests` at the foot
  of a file: it sits on an `impl`, on a single match arm, on a `pub(crate) mod fake`, on individual
  functions — seven files carry more than one. So there is no "region" a line scan can find, and
  brace-counting a file that is full of `{}` inside string literals fails open, which is the
  direction a gate must never fail in.
- **What it does instead is the shape `deny.toml` now uses**: two files are permitted by name with a
  written reason, everything else is refused outright, and a permitted file must additionally hold
  exactly one `#[cfg(test)]` with every OS `cfg` after it. That last clause is the one that keeps the
  permission honest — one `#[cfg(test)]` is what makes "after" mean anything, so a permitted file
  that grows a second fails rather than quietly widening into the product.
- **`cfg!` is deliberately not caught, and that is not a gap.** The tree holds about forty
  `cfg!(windows)` in production code, and they are a different thing: a value, with both arms
  compiled and type-checked and reachable from a test on every OS — which is what "cross-platform or
  not merged" asks for. `#[cfg(windows)]` deletes code from the build, so the other two platforms
  compile a different program and no test on them can say anything about it. The distinction is now
  written in [../standards/rust.md](../standards/rust.md) under "Platform code", along with
  `recipes/mysql.rs`'s better answer again: take `windows` as a function argument, and one table is
  exercised on every runner rather than half of it on each.
- The finding's count reproduces exactly — five `#[cfg]`s, in `recipes/postgres.rs` and
  `install/archive.rs`, all inside test modules.

### R15 — The release signing key lives in a working tree `[x]`

- `mixengine-packages/minisign.key` (encrypted, ignored by `*.key`) sits next to `minisign.pub`.
  Not a leak, but it is the **only** release key; one `git clean -fdx`, one folder-copying tool, or
  one `rm -rf` on an unset variable (which has happened to this repository once) loses or exposes
  it.
- Suggestion: move it outside every repository (`~/.config/mixengine/` or a password manager) and
  point `release/publish.sh` at it through an environment variable.
- **Fixed.** The key was verified in place first — signed a throwaway file and checked it against
  `minisign.pub` — then moved to `~/.config/mixengine/`, outside every working tree. The
  documentation half is `mixengine-packages@17b3dff`, `docs(release): say where the signing key is,
  now that it is not here`.
- **The second half of the suggestion does not apply: `release/publish.sh` does not sign anything.**
  It dispatches `publish-index.yml`, and that workflow is the only thing that signs — it writes
  `secrets.MINISIGN_SECRET_KEY` to a file, signs, `shred -u`s it, and then runs
  `minisign -V -m dist/index.json -p minisign.pub`. That last line is worth naming: it proves on
  every publish that the secret matches the public key committed in the tree, which is the key
  compiled into MixEngine. A wrong secret fails the build rather than publishing an index nobody can
  check.
- So what sat in the working tree was never the thing that signs — it was the **backup**, which is
  the worse fact rather than a milder one. The only place in the repository that named the path was
  a hand-run example in `docs/adding-a-version.md`, in a block whose own preamble says "in practice
  none of that is run by hand"; it now names `$MINISIGN_KEY`.
- **The `.gitignore` comment was part of the problem** and is corrected. It said the key "belongs in
  this repository's Actions secrets and nowhere else" — which reads as *there is no other copy*, and
  is exactly the belief under which a machine ends up holding the only backup in a directory
  `git clean -fdx` sweeps. It now says: the secrets, which is what signs, and a backup outside every
  working tree.
- **What is still true and is not this finding's to close:** there are two copies, on one disk and
  in one GitHub repository, and losing both is an application release rather than a regenerate,
  because the public key is compiled into every installed MixEngine. `docs/the-archive.md` now says
  so, and says that a copy on one disk is not a backup. A third copy somewhere that is neither is
  the user's to make.

### R16 — CI on branches is opt-in; nothing mechanical blocks a merge that is not green `[-]`

- `ci.yml:33-35` triggers on `push: [master]` + `workflow_dispatch` only; no `pull_request`. A
  deliberate decision, with its reasoning in the file (a runner per WIP push is waste; a branch asks
  once, when ready).
- Recorded so nobody "fixes" it without reading the reason. Status `[-]`: accepted, on the
  condition that the "push → request CI → watch-ci → PR → squash" routine keeps being followed by
  hand. Revisit if a second person starts committing.

---

## Done well

What to preserve when refactoring. No checkboxes — these are invariants, not work.

- **Real IPC peer authentication on both platforms.** Unix compares `SO_PEERCRED`/`LOCAL_PEERCRED`
  on every accept ([unix/ipc.rs:136-152](../../crates/mixengine-platform/src/unix/ipc.rs#L136-L152)),
  does not special-case root, and says why. Windows uses *impersonation* rather than
  `GetNamedPipeClientProcessId` — the only pid-free way to ask — holds it across two synchronous
  calls with nothing awaited, and `abort()`s the process if `RevertToSelf` fails
  ([windows/ipc.rs:369-372](../../crates/mixengine-platform/src/windows/ipc.rs#L369-L372)) rather
  than returning a tokio worker to the pool wearing a stranger's token. Rarely done right.
- **`mixengine-elevate` genuinely distrusts its caller.** The request is a file, not argv or a pipe.
  [request.rs](../../crates/mixengine-elevate/src/request.rs) checks, in order: symlink refused
  before any read (`:48`), caller identity taken from the *filesystem* rather than the document
  (`:56`), superuser-owned (`:61`) and world-writable (`:68`) files refused, an existing response
  file as stateless anti-replay (`:80`), home owned by the caller (`:114`), request inside the home
  (`:125`). The TOCTOU between the metadata checks and `read_to_string` (`:87`) is not exploitable,
  because every security-relevant field is re-derived from the *parsed* document. There is no
  `Exec` variant; hosts writes accept only `127.0.0.1`/`::1` (even `127.0.0.2` is rejected),
  domains in `MANAGED_TLDS`, capped at 512 lines. The audit log is root-owned and **outside**
  `MIXENGINE_HOME`, with the rename/unlink reasoning at
  [audit.rs:1-20](../../crates/mixengine-elevate/src/audit.rs#L1-L20). The macOS `osascript` takes a
  compile-time-constant script with paths passed as `argv`.
- **The supply-chain order is correct.** The signature is verified *before* the JSON is parsed
  ([index.rs:308-326](../../crates/mixengine-core/src/index.rs#L308-L326)), the legacy minisign
  algorithm refused; rollback blocked on `generated_at` (`:245-256`); SHA-256 checked before unpack
  (`install.rs:239` → `:247`), a mismatch deletes the download; path traversal blocked twice — a
  component scan ([archive.rs:111](../../crates/mixengine-core/src/install/archive.rs#L111)) and
  then `enclosed_name`/`unpack_in` (`:134`, `:193`); `set_unpack_xattrs(false)`. `minisign-verify`
  in the binary, `minisign` (the signing half) only in testkit; `ruzstd` reads, the C `zstd` only in
  testkit — what the product reads is proved against what a *different implementation* wrote.
- **Secrets never touch argv, generated config, or logs.** Passwords are late-resolved
  `EnvValue::Keyring` references
  ([recipes/mariadb.rs:751-753](../../crates/mixengine-core/src/generate/recipes/mariadb.rs#L751-L753));
  MySQL uses a narrowly permissioned, always-removed `SecretFile` with a redacting `Debug`
  ([first_run.rs:133](../../crates/mixengine-daemon/src/services/first_run.rs#L133)); Postgres goes
  through stdin. A test asserts the invariant (`recipes/mysql.rs:804-805`).
- **DNS is loopback-only**, `recursion_available = false`
  ([dns/server.rs:70](../../crates/mixengine-daemon/src/dns/server.rs#L70), `:121`) — no
  amplification path off the machine. `hickory-server` with no default features, no resolver, no
  second SQLite.
- **A panic is a value, not a dead daemon.** Every RPC runs in its own task
  ([rpc.rs:165-180](../../crates/mixengine-daemon/src/api/rpc.rs#L165-L180)); `panic = "abort"` is
  explicitly forbidden in `Cargo.toml` with the reason; the `daemon.__panic` handler is gated
  behind `#[cfg(test)]`.
- **Near-perfect async hygiene.** No `block_on`, `blocking_lock`, `thread::sleep`, or blocking sqlx
  anywhere in daemon+core; every synchronous I/O is wrapped in a documented `spawn_blocking`. The
  single exception is R5.
- **End-to-end tests against real things, not mocks.** A real daemon spawned from
  `CARGO_BIN_EXE_mixengined`, dialled through `Connection::connect` on a real socket
  ([api.rs:58](../../crates/mixengine-daemon/tests/api.rs#L58)); real runtimes downloaded and
  executed in CI on all three OSes (caddy, mariadb, redis, …); the `bench` job runs under
  `dbus-run-session` + `gnome-keyring-daemon`. Every `#[ignore]` (42, not 75 — the larger figure
  counts doc-comment mentions) carries a reason and, except for R2/R3, a named CI step.
- **CI gates are complete and have no shortcuts.** fmt, clippy `-D warnings` all-targets
  all-features, rustdoc `-D warnings` run under `if: success() || failure()` so it cannot be
  masked, cargo-deny, sqlx offline `--check`, the `mixengine-elevate` dependency budget diffed
  against a committed file, a count that `dbus-secret-service` appears exactly once. No
  `continue-on-error`, no `|| true`, no `--skip`.
- **Architecture enforced by a test.**
  [workspace_layering.rs](../../crates/mixengine-proto/tests/workspace_layering.rs) reads
  `cargo metadata`, compares the member set with `ALLOWED_EDGES` (a new crate cannot slip in),
  rejects upward edges, rejects self-dev-deps, and keeps testkit dev-only. "No client-only
  capability" holds at 59/59 methods; logs are the one exception, and they have an ADR.
- **Restrained, reasoned dependencies.** Daemon 22, core 21, cli 9, platform 6, proto 3, elevate 3
  direct edges; `hickory-server`, `keyring`, `minijinja`, `hyper*`, `sqlx`, `zip` all
  `default-features = false` with the reason beside them; `sqlite-bundled` instead of `sqlite` to
  keep `load-extension` out; `openssl*`/`native-tls`/`winapi` banned.
- **The roadmap matches the code.** No `[x]` task without its code, no `[~]` left hanging; honest
  footnotes (T69 ships off by default, the M3 tail at 11.8 s / 15.1 s on Linux). No comment was
  found to be stale against the code — with 36k comment lines, that is the most notable line in
  this section.
