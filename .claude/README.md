# Moved

The documentation that lived here is under [docs/](../docs/README.md) since T169
([ADR 0043](../docs/decisions/0043-documentation-lives-under-docs.md)). This folder holds only the
agent tooling's configuration: `commands/`, `skills/` and `settings.local.json`.
