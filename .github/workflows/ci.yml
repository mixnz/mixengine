# The eight jobs described in .claude/operations/build-and-release.md: `lint`, `test`, `system`,
# `bench`, `bindings`, `docs`, `desktop` and `build`.
#
# `desktop` arrived with T103, the task that brought the desktop application into this repository
# (ADR 0027).
#
# `system` arrived with the first `#[ignore]`d system test (T40), `build` with T85, the task that
# produced something to install, `bindings` with T56, the task that produced a contract to check, and
# `docs` with T90, the task that produced a site to build. Each was "a green job that proves nothing"
# until then, which is the rule this comment exists to record rather than the list it happens to
# name — and it is why T90's *corpus* invariants added no step anywhere: they are `cargo test`, so
# `test` already runs them on all three operating systems.
#
# Publishing the site is not here at all. It needs `pages: write` and `id-token: write`, and this
# workflow is `contents: read` and stays that way: see `.github/workflows/pages.yml`.
name: CI

# **No branch fires on its own, `master` included.** The `test` job is the only thing in this
# repository that compiles the workspace for all three operating systems, and two thirds of
# `mixengine-platform` is behind a `#[path]` that a developer machine never builds — so a change that
# breaks Linux or macOS is invisible until something compiles it there. That answer is worth a
# runner when somebody is asking for it, and a push is not the same thing as asking: a merge, a
# documentation fix and a work-in-progress save all look identical to a `push` trigger, and only one
# of them wanted an answer.
#
# So every ref asks for itself, `master` the same as any other. Push, then request a run on it:
#
#     git push origin HEAD
#     gh workflow run ci.yml --ref "$(git branch --show-current)"
#
# The run is labelled with the ref that asked, so two questions in flight are told apart by their ref
# instead of overwriting each other on one shared lane.
#
# **A tag is the one exception**, and it is not a branch. Pushing `v*` fires the run that *is*
# release-checklist item 1 — "all CI green on all three OSes" — and then `preflight` and `release` at
# the bottom of this file: sign what `build` made, and assemble a draft somebody publishes. That one
# has to be automatic, because a tag whose run was never requested is a release nobody checked.
#
# The dispatch list is read from the default branch: a workflow file only becomes requestable once
# this file is on `master`, and a branch that edits it runs its own version once selected.
#
# Pull requests are not a trigger. A pull request's head is a branch you can request a run on like
# any other, and leaving `pull_request` on would mean the same commit builds twice.
on:
  push:
    tags: ['v*']
  workflow_dispatch:

# A second request on the same ref makes the previous run pointless. A feature branch's run is
# cancelled and replaced, because asking again means the answer in flight is about a commit you have
# already moved past, and waiting it out delays the one you now want. Runs on `master` are kept
# anyway, so that ref keeps a complete result history however the runs were started.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # A tag joins master in never being cancelled (T86). A cancelled release run leaves a half-uploaded
  # draft, which is the one state that pipeline has no answer for.
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' && !startsWith(github.ref, 'refs/tags/') }}

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  # CI builds are never incremental: it only costs cache size and hurts a cold runner.
  CARGO_INCREMENTAL: 0
  RUST_BACKTRACE: 1

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    # Generous, but far short of the six-hour default: a job that hangs should free the runner.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      # No toolchain action: rust-toolchain.toml is the single source of truth for the version and
      # for the clippy/rustfmt components. Given no argument, `rustup toolchain install` installs
      # exactly what that file pins — note that `rustup show` stopped installing anything in 1.28.
      - name: Install pinned toolchain
        run: |
          rustup toolchain install --no-self-update
          rustup show active-toolchain

      - uses: Swatinem/rust-cache@v2
        with:
          key: lint

      - name: rustfmt
        run: cargo fmt --all --check

      - name: clippy
        run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings

      # D8 of the T40 design. `mixengine-elevate` runs as root, and its whole dependency closure is a
      # security decision — its own manifest says so. Cargo unifies features across a workspace build,
      # so `cargo build --workspace` compiles `mixengine-platform` with tokio and keyring and links
      # the helper against that; the lean tree is a property of building the helper **on its own**,
      # which is what both of these steps do and what the release pipeline does.
      #
      # A change here is not a lint failure to be waved through: read the diff, and if the new
      # dependency belongs in a binary that runs as root, commit the updated list in the same change.
      - name: The elevated helper builds without the rest of the platform
        run: cargo check -p mixengine-elevate --locked

      - name: The elevated helper's dependency budget
        run: |
          set -euo pipefail
          cargo tree -p mixengine-elevate --no-default-features --edges normal             --prefix none --format '{p}' | sed 's/ v[0-9].*//' | sort -u > /tmp/elevate-dependencies.txt
          grep -v '^#' .github/elevate-dependencies.txt | diff -u - /tmp/elevate-dependencies.txt

      # ADR 0013. `linux/secrets.rs` reads a machine with no secret service by downcasting `keyring`'s
      # boxed source to `dbus_secret_service::Error`, which works only while our direct dependency and
      # the one `keyring` resolves are the **same** package. Two versions in one tree is not a build
      # failure and not a test failure: the downcast simply answers `None` for ever, and every machine
      # without a keyring goes back to being told its store refused.
      #
      # So it is counted rather than trusted. The day `keyring` moves to a version we do not follow,
      # this step is what says so — and the fix is to follow it in the same change, not to widen the
      # match here.
      #
      # **`--color never` is load-bearing.** This job sets `CARGO_TERM_COLOR: always`, so a repeated
      # subtree's `(*)` marker arrives wrapped in escape sequences and the `sed` below stops matching
      # it — which counts one package as two and fails a tree that is fine. Locally the same command
      # passes, because cargo drops colour on its own when it is not writing to a terminal.
      - name: One dbus-secret-service in the tree, or the Linux keyring reading is silently dead
        run: |
          set -euo pipefail
          found=$(cargo tree --workspace --locked --color never --target x86_64-unknown-linux-gnu             --prefix none --format '{p}' | sed 's/ (\*)$//'             | grep '^dbus-secret-service ' | sort -u)
          echo "$found"
          test "$(printf '%s\n' "$found" | grep -c .)" -eq 1

      # `sqlx::query!` checks its SQL against a real database at compile time. Nobody building
      # MixEngine has one — so the answers are committed as `.sqlx/`, and every other build reads
      # those instead of connecting (T14). The failure this step exists for is a query edited
      # without re-running `prepare`: the author's machine still builds, because DATABASE_URL is set
      # there, and everyone else's stops. `--check` regenerates the answers and fails if they differ
      # from what is committed.
      #
      # Prebuilt binary rather than `cargo install sqlx-cli`, which is a four-minute compile of a
      # tool this job uses for six seconds.
      #
      # Pinned to the `sqlx` version in Cargo.lock, and it has to stay in step with it: the contents
      # of `.sqlx/` are a version-specific format, so an unpinned CLI would start regenerating them
      # differently the day sqlx releases and fail `--check` on whichever unrelated PR ran next.
      - name: Install sqlx-cli
        uses: taiki-e/install-action@v2
        with:
          tool: sqlx-cli@0.9.0

      - name: sqlx offline data is current
        env:
          DATABASE_URL: sqlite:target/sqlx-ci.db
        run: |
          cargo sqlx database create
          cargo sqlx migrate run --source crates/mixengine-core/migrations
          cargo sqlx prepare --workspace --check -- --all-targets --all-features

      # rustdoc runs in the `test` job instead, once per runner — see the note there. It has to be
      # built on the OS it documents, and this job only has one.
      #
      # Licences and advisories. Configuration lives in deny.toml, including the six release targets.
      - name: cargo-deny
        uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check
          # `check` alone would also run `sources`, which is fine, but being explicit means a new
          # check category added upstream cannot silently start failing our builds.
          arguments: --all-features
          command-arguments: advisories bans licenses sources

      # T86's D9. `packaging/sign.sh` drives an external tool, and the only other thing that would
      # ever run it is a release — so it is run here, on every CI run, against a throwaway key.
      #
      # In `lint` rather than in `test` for two reasons: it is a check on this repository and the
      # tools it uses, which is what every step above it is; and `test` runs on three operating
      # systems with network egress blocked, where installing minisign would be three problems in
      # exchange for two answers nobody needs.
      - name: Install minisign
        run: |
          sudo apt-get update
          sudo apt-get install --yes minisign

      - name: The signing script signs everything, and refuses a key that is not the product's
        run: bash .github/scripts/test-sign.sh

      # T88. The other half of what a release depends on and no ordinary run exercises: the document
      # every installed copy reads to find out that a newer one exists. Same reasoning as the step
      # above — the only other thing that would ever run `feed.sh` is a release.
      - name: The feed describes the archives beside it
        run: bash .github/scripts/test-feed.sh

  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    # **45 since phase 11, and it is the line past which waiting stops being the answer, not a
    # budget.** The Windows leg took 24.5 minutes warm on master (run 34149551423) and was cut off
    # at 30 on the first branch to change the toolchain pin (run 34235885258): a new pin is a new
    # cache key, `Build tests` went from 313 s to 602 s with nothing to restore, and every suite ran
    # a third slower on that runner. Nothing hung — the leg was killed installing PostgreSQL, the
    # ninth of eleven suites. The same reasoning as `system`'s 45.
    timeout-minutes: 45
    strategy:
      # One OS failing must not hide the result on the other two — the platform layer is exactly
      # where per-OS divergence shows up.
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      # T2b, answered: this leg holds `BUILTIN\Administrators` as an *enabled* group at High
      # Mandatory Level — a full token, not the UAC-filtered one where that group is present
      # deny-only and grants nothing. The account name in the cache paths, `runneradmin`, never
      # settled that; only `/groups` does.
      #
      # It stays as an assertion rather than being deleted with the question, because
      # standards/testing.md now states the answer and derives a rule from it — write Windows
      # exclusion tests structurally, never by attempting an access this token would win — and a
      # runner image that quietly de-escalated would leave that page confidently wrong. Failing on
      # the good news is the point: the news is only good once somebody acts on it.
      - name: What this leg runs as (T2b)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $groups = whoami /groups
          $groups
          $admin = $groups | Select-String -SimpleMatch 'S-1-5-32-544'
          if (-not ($admin -and "$admin" -match 'Enabled group')) {
            Write-Host '::error::This leg is no longer an elevated administrator, which is good news and not a build failure. Re-read the Privilege section of .claude/standards/testing.md, which concluded the opposite, and delete this step with it.'
            exit 1
          }

      - name: Install pinned toolchain
        run: |
          rustup toolchain install --no-self-update
          rustup show active-toolchain

      - uses: Swatinem/rust-cache@v2
        with:
          key: test
          # Saved by a leg that failed or was cut off too — the action's default saves only from a
          # green job, which is how a cold cache and a timeout feed each other: the first cold leg is
          # cut off, saves nothing, and the rerun is cold again (run 34235885258, and the two before
          # it on the same branch). A cache is a record of what compiled, and a red test says nothing
          # about that.
          cache-on-failure: true

      # Everything the build needs is downloaded here, while the network is still allowed. From this
      # point on cargo runs offline, so a dependency that is not in Cargo.lock fails the job instead
      # of being fetched silently.
      - name: Fetch dependencies
        run: cargo fetch --locked

      # The one thing in this job that is downloaded rather than built, and the reason it is worth
      # it: `crates/mixengine-cli/tests/caddy.rs` is the only test in the workspace that judges a
      # recipe against the program it configures. Everything else about T31 is provable in one
      # process; that Caddy *accepts what MixEngine generates* is not, and a template with a Windows
      # path in it is exactly the kind of thing that is fine on two systems and broken on the third.
      #
      # From our own signed index's release rather than from upstream, pinned, so this leg installs
      # the same artifact a user would. It is a **test fixture and not an install**: nothing here
      # checks a signature or a hash, because the code that does is `core::index` and `core::install`
      # and both have their own suites. What would break if the download were wrong is this test,
      # loudly.
      #
      # Before the offline steps below, because from there on there is no network — and on Linux no
      # route out at all.
      - name: Fetch a real Caddy
        env:
          CADDY: "2.11.4"
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            Linux-X64)     target=linux-x86_64;    ext=tar.zst ;;
            Linux-ARM64)   target=linux-aarch64;   ext=tar.zst ;;
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            Windows-ARM64) target=windows-aarch64; ext=zip ;;
            *) echo "::error::mixengine-packages publishes no Caddy for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/caddy"
          archive="$RUNNER_TEMP/caddy.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive" \
            "https://github.com/mixnz/mixengine-packages/releases/download/caddy-$CADDY/caddy-$CADDY-$target.$ext"

          # Two different `tar`s, and on Windows the one on the PATH is the wrong one. Git Bash
          # ships GNU tar, which reads `D:\a\_temp\caddy.zip` as a *remote host* called `D` — and
          # cannot read a zip even once told otherwise. Windows itself ships bsdtar, which reads
          # both, so it is named outright rather than reached through the PATH.
          #
          # Elsewhere the archive is `.tar.zst`: bsdtar decompresses it by magic and GNU tar shells
          # out to `zstd`, so the pipe is the fallback for a tar built without it.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_CADDY_PACKAGE=$into" >> "$GITHUB_ENV"
          "$into/caddy$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" version

      # **And the other front end — T37.** The same reasoning as the Caddy step, plus one thing only
      # this program can settle: an `include` inside an nginx configuration resolves against the
      # *prefix* rather than against the file, so whether `include sites/*.conf` is judged in the
      # staging directory — which is what makes a broken rendering refused before it is installed —
      # is a question about nginx's own path handling and about nothing MixEngine can assert.
      #
      # **The whole tree, not the binary.** A generated `nginx.conf` includes the archive's own
      # `conf/mime.types` by absolute path, so the fixture packs what was unpacked here.
      - name: Fetch a real nginx
        shell: bash
        env:
          NGINX: "1.31.3"
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            Linux-X64)     target=linux-x86_64;    ext=tar.zst ;;
            Linux-ARM64)   target=linux-aarch64;   ext=tar.zst ;;
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            Windows-ARM64)
              echo "::notice::nginx.org publishes one Windows build and it is 32-bit x86, so this cell is empty on purpose"
              exit 0 ;;
            *) echo "::error::mixengine-packages publishes no nginx for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/nginx"
          archive="$RUNNER_TEMP/nginx.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive" \
            "https://github.com/mixnz/mixengine-packages/releases/download/nginx-$NGINX/nginx-$NGINX-$target.$ext"

          # The same two `tar`s for the same reason as the Caddy step above.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_NGINX_PACKAGE=$into" >> "$GITHUB_ENV"
          # `-v` and not `-t`: the second reads a configuration, and there is none here. It is also
          # what the recipe's smoke test runs, so a build that fails this fails an install too.
          "$into/nginx$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" -v

      # **A second real program, and it buys what the Caddy one cannot.** Caddy is one binary that
      # behaves the same on all three systems; PHP is the opposite, and T32 is the task about that:
      # a Unix artifact publishes `php-fpm` and a Windows one publishes `php-cgi`, which are
      # different SAPIs configured by different mechanisms — a file on one, an environment on the
      # other. A recipe that renders correctly on both is not a recipe that runs on both, and only
      # the program can tell the two apart.
      - name: Fetch a real PHP
        env:
          PHP: "8.3.33"
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            Linux-X64)     target=linux-x86_64;    ext=tar.zst ;;
            Linux-ARM64)   target=linux-aarch64;   ext=tar.zst ;;
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            Windows-ARM64) target=windows-aarch64; ext=zip ;;
            *) echo "::error::mixengine-packages publishes no PHP for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/php"
          archive="$RUNNER_TEMP/php.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive"             "https://github.com/mixnz/mixengine-packages/releases/download/php-$PHP/php-$PHP-$target.$ext"

          # The same two `tar`s for the same reason as the Caddy step above.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_PHP_RUNTIME=$into" >> "$GITHUB_ENV"
          # `bin/php` on Unix, `php.exe` at the root on Windows — the layout is the publisher's, and
          # the suite probes for it rather than writing it down. This is only the smoke check.
          "$into/bin/php$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" --version ||             "$into/php$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" --version

      # And a real MariaDB, for T33 — the first recipe that has to *create* something before it can
      # run at all, and the one whose every platform difference is upstream's rather than ours: two
      # different `mariadb-install-db` programs, a bootstrap that refuses `SET PASSWORD`, and an
      # option parser that reads a backslash as an escape.
      #
      # **A cost worth stating.** MariaDB is much larger than Caddy or PHP, and this adds a download
      # plus a real bootstrap to every `test` job on every runner. If it pushes CI past what is
      # tolerable, the answer is a separate job rather than a quieter test.
      - name: Fetch a real MariaDB
        env:
          MARIADB: "11.4.12"
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            Linux-X64)     target=linux-x86_64;    ext=tar.zst ;;
            Linux-ARM64)   target=linux-aarch64;   ext=tar.zst ;;
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            Windows-ARM64) target=windows-aarch64; ext=zip ;;
            *) echo "::error::mixengine-packages publishes no MariaDB for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/mariadb"
          archive="$RUNNER_TEMP/mariadb.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive" \
            "https://github.com/mixnz/mixengine-packages/releases/download/mariadb-$MARIADB/mariadb-$MARIADB-$target.$ext"

          # The same two `tar`s for the same reason as the Caddy step above.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_MARIADB_PACKAGE=$into" >> "$GITHUB_ENV"
          # `bin/mariadbd` on every system — the layout is the publisher's, and the suite probes for
          # it rather than writing it down. This is only the smoke check.
          "$into/bin/mariadbd$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" --version

      # And a *second* MariaDB, of another series, for T36 — two instances of one server, at two
      # versions, running side by side. 10.6 is the oldest line the index publishes and is chosen
      # for what it does not share with 11.4: upstream renamed every program the bootstrap runs
      # between the two, and their `share/` layouts differ. Two instances of one version would
      # share a `packages` row and prove only that two directories can have two names.
      #
      # **The cost is a second download and a second bootstrap**, and it is why the suite that uses
      # it is its own step: if that becomes intolerable, the answer is a job of its own rather than
      # a suite that stops running two servers.
      - name: Fetch a second MariaDB, of an older series
        env:
          MARIADB_LEGACY: "10.6.28"
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            Linux-X64)     target=linux-x86_64;    ext=tar.zst ;;
            Linux-ARM64)   target=linux-aarch64;   ext=tar.zst ;;
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            Windows-ARM64) target=windows-aarch64; ext=zip ;;
            *) echo "::error::mixengine-packages publishes no MariaDB for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/mariadb-legacy"
          archive="$RUNNER_TEMP/mariadb-legacy.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive" \
            "https://github.com/mixnz/mixengine-packages/releases/download/mariadb-$MARIADB_LEGACY/mariadb-$MARIADB_LEGACY-$target.$ext"

          # The same two `tar`s for the same reason as the Caddy step above.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_MARIADB_LEGACY_PACKAGE=$into" >> "$GITHUB_ENV"
          # This series ships `bin/mysqld` and a `bin/mariadbd` beside it; the suite probes for
          # every program it needs and this is only the smoke check.
          "$into/bin/mariadbd$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" --version

      # And a real MySQL, for T34c — the other database with these programs' names, and not a
      # version of the one above. 8.4 is the line `.claude/features/services.md` names; the two 5.x
      # lines take the other two bootstrap routes and are judged by `mixengine-packages`' own smoke
      # test on every published cell, which is where the artifacts that need them are made.
      #
      # **There is no Windows-on-ARM cell in any MySQL line** — upstream has never published one and
      # nothing here compiles Windows — so that leg fetches nothing and the test step below skips
      # itself rather than failing on a download that could not succeed.
      - name: Fetch a real MySQL
        shell: bash
        env:
          MYSQL: "8.4.10"
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            Linux-X64)     target=linux-x86_64;    ext=tar.zst ;;
            Linux-ARM64)   target=linux-aarch64;   ext=tar.zst ;;
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            Windows-ARM64)
              echo "::notice::mixengine-packages publishes no MySQL for Windows on ARM, and neither does upstream"
              exit 0 ;;
            *) echo "::error::mixengine-packages publishes no MySQL for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/mysql"
          archive="$RUNNER_TEMP/mysql.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive" \
            "https://github.com/mixnz/mixengine-packages/releases/download/mysql-$MYSQL/mysql-$MYSQL-$target.$ext"

          # The same two `tar`s for the same reason as the Caddy step above.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_MYSQL_PACKAGE=$into" >> "$GITHUB_ENV"
          # `bin/mysqld` on every system this fetches. Only the smoke check; the suite probes.
          "$into/bin/mysqld$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" --version

      # And a real PostgreSQL, for T34 — the recipe that made T34a necessary. It is the smaller of
      # the two databases, and it is the one whose Windows leg only runs at all because a supervised
      # child is now created from a restricted token: `postgres` refuses the full token the step at
      # the top of this job asserts this runner holds.
      - name: Fetch a real PostgreSQL
        env:
          POSTGRES: "18.6"
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            Linux-X64)     target=linux-x86_64;    ext=tar.zst ;;
            Linux-ARM64)   target=linux-aarch64;   ext=tar.zst ;;
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            # Upstream does not compile PostgreSQL there before 19, so the index offers nothing on
            # this cell and neither does this step. The suite below is skipped rather than failed.
            Windows-ARM64) echo "::notice::mixengine-packages publishes no PostgreSQL for Windows on ARM; the recipe suite is skipped on this leg"; exit 0 ;;
            *) echo "::error::mixengine-packages publishes no PostgreSQL for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/postgres"
          archive="$RUNNER_TEMP/postgres.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive" \
            "https://github.com/mixnz/mixengine-packages/releases/download/postgres-$POSTGRES/postgres-$POSTGRES-$target.$ext"

          # The same two `tar`s for the same reason as the two steps above.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_POSTGRES_PACKAGE=$into" >> "$GITHUB_ENV"
          # `bin/postgres` on every route — one spelling, which the Debian cell buys with a symlink.
          "$into/bin/postgres$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" --version

      # And the two caches, for T35. Small downloads next to the two databases above, and the pair
      # of them is one roadmap task because neither is big enough to be one on its own.
      - name: Fetch a real Redis
        env:
          REDIS: "8.10.0"
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            Linux-X64)     target=linux-x86_64;    ext=tar.zst ;;
            Linux-ARM64)   target=linux-aarch64;   ext=tar.zst ;;
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            # Cygwin has no aarch64 port, so the Windows cells of both caches are built for x86_64
            # and that one alone — see mixengine-packages' `docs/packages/redis-memcached.md`.
            Windows-ARM64) echo "::notice::mixengine-packages publishes no Redis for Windows on ARM; the recipe suite is skipped on this leg"; exit 0 ;;
            *) echo "::error::mixengine-packages publishes no Redis for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/redis"
          archive="$RUNNER_TEMP/redis.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive" \
            "https://github.com/mixnz/mixengine-packages/releases/download/redis-$REDIS/redis-$REDIS-$target.$ext"

          # The same two `tar`s for the same reason as the steps above.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_REDIS_PACKAGE=$into" >> "$GITHUB_ENV"
          "$into/bin/redis-server$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" --version

      - name: Fetch a real memcached
        env:
          MEMCACHED: "1.6.45"
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            Linux-X64)     target=linux-x86_64;    ext=tar.zst ;;
            Linux-ARM64)   target=linux-aarch64;   ext=tar.zst ;;
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            Windows-ARM64) echo "::notice::mixengine-packages publishes no memcached for Windows on ARM; the recipe suite is skipped on this leg"; exit 0 ;;
            *) echo "::error::mixengine-packages publishes no memcached for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/memcached"
          archive="$RUNNER_TEMP/memcached.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive" \
            "https://github.com/mixnz/mixengine-packages/releases/download/memcached-$MEMCACHED/memcached-$MEMCACHED-$target.$ext"

          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_MEMCACHED_PACKAGE=$into" >> "$GITHUB_ENV"
          # `-V` and not `--version`: the short flag is in every line the index publishes.
          "$into/bin/memcached$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" -V

      - name: Build tests
        run: cargo test --workspace --all-targets --all-features --locked --offline --no-run

      # The keyring capability talks to a D-Bus secret service on Linux, and a runner has a session
      # bus with no provider on it — which since T15b the tests read for what it is, a machine with
      # no credential store, and therefore **skip**. Supplying a real one is what turns those eight
      # tests back into coverage; the run that proves the skipping branch itself is the separate
      # "no secret service" step below, which takes the store away on purpose and asserts it is
      # gone. Installed as a job step rather than from the script: the suite
      # runs in a namespace with no route out, so nothing inside it can fetch a package. Starting
      # the daemon is the script's job, since it has to happen inside that namespace.
      #
      # Windows and macOS need no equivalent: their stores are part of the OS.
      #
      # **Bounded, because an unbounded one cost a whole run.** `apt-get` waits on a stalled mirror
      # for as long as the mirror likes: one run spent twenty-seven minutes inside `apt-get update`
      # and was killed by the job's own timeout, having proved nothing about anything. A per-request
      # deadline and three attempts is what a mirror having a bad minute deserves; a step timeout
      # well under the job's is what says *this* is what went wrong when it is having a bad hour.
      #
      # **And `libnss3-tools`, which is T49b's whole starting measurement.** `certutil` is not
      # installed on a stock Ubuntu 24.04 and `libnss3` does not pull it in, so the round trip in
      # `crates/mixengine-platform/tests/browsers.rs` is `#[ignore]`d and this is the only leg that
      # runs it. About 2 MB, and in this step rather than one of its own so that the job holds one
      # `apt-get update` and not two.
      - name: Install the packages this leg's tests need
        if: runner.os == 'Linux'
        timeout-minutes: 6
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          set -euo pipefail

          for attempt in 1 2 3; do
            sudo apt-get -o Acquire::Retries=3 -o Acquire::http::Timeout=20 -o Acquire::https::Timeout=20 update && break
            echo "::warning title=apt-get update failed::attempt $attempt of 3"
            sleep 5
          done

          sudo apt-get install -y --no-install-recommends gnome-keyring dbus libnss3-tools

      # Linux is the leg that actually proves "no network in tests": the suite runs in a namespace
      # with nothing but loopback. The script runs the doc tests too, inside the same namespace.
      - name: Test (no network)
        if: runner.os == 'Linux'
        env:
          CARGO_NET_OFFLINE: "true"
        run: bash .github/scripts/test-no-network.sh

      # T15b. Every other leg of this job hands the tests a store that works, on purpose — so the
      # answer a machine *without* one gets is a branch every green run steps past, which is how the
      # bug this step exists for lived long enough to be found by a stack trace on somebody's
      # console. This takes the store away in the three ways a Linux can lose it and asserts, rather
      # than skips: `MIXENGINE_TEST_NO_KEYRING=1` makes finding a store a failure, so a round whose
      # sabotage silently did not work says so instead of passing.
      #
      # Outside the network namespace deliberately. The point here is which D-Bus the tests reach,
      # and the script builds a bus of its own to control exactly that.
      - name: Test the answer a machine with no secret service gets
        if: runner.os == 'Linux'
        run: bash .github/scripts/test-absent-secret-service.sh

      # The suite that needs a real `certutil` — T49b. `#[ignore]`d rather than skipped, in the
      # shape every real-program suite in this job uses: a machine without `libnss3-tools` says the
      # test did not run instead of quietly reporting a pass, and this is the only leg that has the
      # package.
      #
      # Outside the network namespace, deliberately: nothing here reaches a network, and the
      # database it writes into is one it made in a temp directory of its own — which is also what
      # keeps `.claude/standards/testing.md`'s first rule, since no real browser profile is touched.
      - name: Test against a real certutil
        if: runner.os == 'Linux'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-platform --test browsers --locked --offline -- --ignored

      # Windows and macOS have no equally cheap isolation primitive, so there the rule is carried by
      # the test harness (T11) plus the offline cargo above.
      # **`--no-fail-fast`, so a leg reports every failure it found rather than the first.** cargo
      # stops after the first failing test binary by default, which on a matrix means one answer per
      # round trip — and a round trip here is three operating systems and ten minutes. The suites
      # after the first are exactly the ones that say whether a failure is one test or all of them.
      - name: Test
        if: runner.os != 'Linux'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test --workspace --all-targets --all-features --locked --offline --no-fail-fast

      # T69's connection count, against a connection that really is established. `01`,
      # `MIB_TCP_STATE_ESTAB` and `-sTCP:ESTABLISHED` are three claims about three unrelated
      # mechanisms, and the captured tables the unit tests parse prove the parsing and say nothing
      # about the constant — so a socket somebody is on the other end of is the only thing that can
      # check any of them, and CI is where all three of those mechanisms exist.
      #
      # **This job and not `system`**, which is what the suite's own note used to claim: counting
      # connections needs no administrative token, and `system` is the job for what an unprivileged
      # process cannot prove. What this needs is a real socket on each of the three operating
      # systems, and this is the job that runs on all three. `#[ignore]`d and given a step rather
      # than left to the workspace run, in the certutil step's shape: a leg where it did not run
      # says so instead of quietly reporting a pass.
      #
      # On Linux outside the network namespace, deliberately. Inside it the tables hold this test's
      # own sockets and nothing else; picking one port out of the machine's whole table is the
      # harder question, and it is the one the idle sweeper asks.
      - name: Test against a real connection
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-platform --test connections --locked --offline -- --ignored --nocapture

      # The suite that needs the archive above. `#[ignore]`d rather than skipped, so a system where
      # it does not run says so instead of reporting a pass — see the note at the top of
      # `crates/mixengine-cli/tests/caddy.rs`. The Linux runner did this inside the namespace
      # already; a Caddy on loopback does not need a route out.
      - name: Test against a real Caddy
        if: runner.os != 'Linux'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test caddy --locked --offline -- --ignored

      # What a *shared* site puts on the network, against the same real Caddy — roadmap task T76.
      # Its own step rather than a second `--test` above, for the reason every suite here has one: a
      # failure should name what failed without anybody reading the log.
      #
      # It proves what is listening rather than what a firewall allows — every connection it makes is
      # to this machine's own address and so never crosses one — which is the half T74's first real
      # run found broken, with `netstat` and not with a firewall. The rule half is
      # `mixengine-core --test firewall`, on the Windows leg below.
      #
      # Not on Linux, for the same reason the Caddy step above is not: that runner does its front-end
      # work inside a network namespace.
      - name: Test what a shared site listens on
        if: runner.os != 'Linux'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test sharing --locked --offline -- --ignored

      # And the other front end through the same arc — T37. Its own step rather than a second
      # `--test` on the line above, for the reason every suite below has one: a failure should name
      # which recipe failed without anybody reading the log. The two run the *same* sequence of
      # assertions from `tests/harness/frontend.rs`, which is what makes them a parity suite rather
      # than two files that resemble each other.
      #
      # The guard on the variable is what would make a Windows-on-ARM leg green: there is no nginx to
      # fetch there and no upstream that publishes one.
      - name: Test against a real nginx
        if: runner.os != 'Linux' && env.MIXENGINE_NGINX_PACKAGE != ''
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test nginx --locked --offline -- --ignored

      # And the one that needs the PHP. Its own step rather than a second `--test` on the line above,
      # because a failure should name which recipe failed without anybody reading the log.
      - name: Test against a real PHP
        if: runner.os != 'Linux'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test php_fpm --locked --offline -- --ignored

      # And the ini set that PHP reads — T28. Its own step for the reason the one above has: a
      # failure should name which half failed without anybody reading the log.
      - name: Test the generated PHP ini set
        if: runner.os != 'Linux'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test php_extensions --locked --offline -- --ignored

      # And the one that needs the MariaDB. Its own step for the reason the PHP one is: a failure
      # should name which recipe failed without anybody reading the log.
      #
      # The Linux leg runs this from inside `test-no-network.sh` rather than here, and the
      # difference is the credential store: the bootstrap refuses a machine without one **by
      # design**, and that script is where a `gnome-keyring` is started on a session bus of its own.
      #
      # **`--nocapture`, and a timeout of its own.** libtest holds a running test's output until the
      # test ends, so the first run of this suite on macOS reported twenty-seven minutes of silence
      # and then the job's own timeout. The suite says what it is doing as it goes; this is what
      # lets a reader see it, and the step timeout is what leaves the doc tests below enough of the
      # job to still run.
      - name: Test against a real MariaDB
        if: runner.os != 'Linux'
        timeout-minutes: 15
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test mariadb --locked --offline -- --ignored --nocapture

      # And the one that runs two of them at once — T36. Its own step rather than more of the one
      # above, because what it proves is a different claim and because it is the expensive half:
      # two installs, two bootstraps, two servers up together. Measured at fifty seconds on a
      # local Linux, against that suite's thirty-six.
      #
      # The Linux leg runs this from inside `test-no-network.sh`, for the reason MariaDB's own
      # suite does: two first-run rituals, two generated root passwords, and a credential store
      # that only exists inside that script's session bus.
      - name: Test two instances of one server, at two versions
        if: runner.os != 'Linux'
        timeout-minutes: 25
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test instances --locked --offline -- --ignored --nocapture

      # And the one that needs the PostgreSQL — T34. Its own step for the reason the three above
      # have one: a failure should name which recipe failed without anybody reading the log.
      #
      # The Linux leg runs this from inside `test-no-network.sh`, for MariaDB's reason: the ritual
      # puts the generated superuser password in the OS credential store and refuses a machine with
      # none, and that script is where a `gnome-keyring` runs on a session bus of its own.
      #
      # **That the Windows leg runs this at all is T34a.** `postgres` refuses a token holding an
      # enabled `BUILTIN\Administrators`, which the step at the top of this job asserts this runner
      # still has; every supervised child is now created from a restricted copy of it —
      # `.claude/decisions/0010-supervised-child-never-inherits-administrators.md`.
      - name: Test against a real PostgreSQL
        if: runner.os != 'Linux' && env.MIXENGINE_POSTGRES_PACKAGE != ''
        timeout-minutes: 15
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test postgres --locked --offline -- --ignored --nocapture

      # And the one that needs the MySQL — T34c. Its own step for the reason every one above has
      # one: a failure should name which recipe failed without anybody reading the log.
      #
      # The Linux leg runs this from inside `test-no-network.sh`, for MariaDB's reason: the ritual
      # stores a generated root password in the OS credential store and refuses a machine with none.
      #
      # The guard on the variable is what makes the Windows-on-ARM leg green: there is no MySQL to
      # fetch there, and a suite that is `#[ignore]`d until a real server exists has nothing to say.
      - name: Test against a real MySQL
        if: runner.os != 'Linux' && env.MIXENGINE_MYSQL_PACKAGE != ''
        timeout-minutes: 15
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test mysql --locked --offline -- --ignored --nocapture

      # And the two caches — T35. Two steps rather than one for the reason the PHP pair are two: a
      # failure should name which recipe failed without anybody reading the log.
      - name: Test against a real Redis
        if: runner.os != 'Linux' && env.MIXENGINE_REDIS_PACKAGE != ''
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test redis --locked --offline -- --ignored --nocapture

      - name: Test against a real memcached
        if: runner.os != 'Linux' && env.MIXENGINE_MEMCACHED_PACKAGE != ''
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test memcached --locked --offline -- --ignored --nocapture

      # `--all-targets` silently excludes doc tests; run them separately so rustdoc examples cannot
      # rot. The Linux runner did this inside the namespace already.
      - name: Doc tests
        if: runner.os != 'Linux'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test --workspace --all-features --locked --offline --doc

      # rustdoc is the only tool that resolves intra-doc links, and nothing else in CI runs it:
      # clippy does not build the docs, and `cargo test --doc` compiles the examples without
      # checking a single link. `--document-private-items` because most of this workspace is
      # private — a link inside a `mod` that is never exported is exactly the kind that rots. It
      # does not make this a documentation-coverage gate: `missing_docs` still exempts private
      # items, checked by deleting one of their doc comments.
      #
      # `rustdoc::all` is denied in the workspace manifest, so this fails on a developer machine
      # too; RUSTDOCFLAGS stays because it also covers the rustc warnings rustdoc's compile pass
      # raises, which no `[lints]` table can express.
      #
      # Once per OS, not once: every OS directory in mixengine-platform is mapped onto `sys` by
      # `#[path]`, so a host-only run leaves two thirds of that crate unbuilt, and the broken link
      # that created T2a was in the macOS half. It lives in this job rather than in `lint` because
      # `--target` stopped being enough the moment T6 added a dependency that compiles C: rustdoc
      # does not link, but cargo still runs `libsqlite3-sys`'s build script, and cross-compiling
      # SQLite needs a toolchain for the foreign OS that no runner has. Reproduced before moving
      # it — `cargo doc --target x86_64-unknown-linux-gnu` from another host fails in cc-rs with
      # "failed to find tool x86_64-linux-gnu-gcc", not in rustdoc.
      #
      # It runs even when a test failed. In `lint` this check was parallel with the tests; as the
      # last step of `test` it would be skipped by the first failing test instead, hiding every
      # broken intra-doc link until that test was fixed — a check that only runs when everything
      # else already passed is not much of a check. `success() || failure()` rather than
      # `always()`, which would also start it after the job had been cancelled.
      - name: rustdoc
        if: success() || failure()
        env:
          RUSTDOCFLAGS: -D warnings
          CARGO_NET_OFFLINE: "true"
        run: >
          cargo doc --workspace --no-deps --document-private-items --all-features --locked
          --offline

  # The job `.claude/operations/build-and-release.md` says arrives with the first `#[ignore]`d system
  # test. T40 is that task: `mixengine-elevate` creates a root-owned audit directory outside
  # MIXENGINE_HOME, and nothing about that can be proved from an unprivileged process.
  #
  # It runs on every run of this workflow rather than only on branches that touch `platform` or
  # `elevate`, which is what that table used to say: the triggers here are `push: master` and
  # `workflow_dispatch`, and a dispatch carries no diff to test a path filter against.
  #
  # **35 minutes and not 20** since the certificate suites joined it. Those build the CLI and the
  # daemon — which this job had no reason to compile before — and then drive a real Caddy through a
  # rotation on each of the three systems. The number is the shape of the job, not a measurement of
  # one run: the steps that could hang rather than fail have clocks of their own.
  #
  # **45 since T87's**, which adds one more suite that drives the CLI and the daemon and then takes
  # the machine apart — with a clock of its own, on the rule above.
  system:
    name: system (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install pinned toolchain
        run: |
          rustup toolchain install --no-self-update
          rustup show active-toolchain

      - uses: Swatinem/rust-cache@v2
        with:
          key: system

      - name: Fetch dependencies
        run: cargo fetch --locked

      # Built as the ordinary user and *run* as root below. `sudo cargo test` would compile a second
      # copy of everything into a target directory root owns, and leave it there for the next job.
      - name: Build the system suite
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-elevate --test system --locked --offline --no-run

      # This leg already holds a full administrator token (T2b, asserted in the `test` job), so there
      # is nothing to raise: running the binary directly is running it elevated.
      # `--test-threads=1`: every test in this suite writes the one audit directory this machine has,
      # which is the whole point of it being a system suite. Run in parallel they interleave on that
      # one directory and assert about each other's half-finished work rather than about the code.
      - name: Elevated suite (Windows)
        if: runner.os == 'Windows'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-elevate --test system --locked --offline -- --ignored --nocapture --test-threads=1

      # `sudo -E` keeps SUDO_UID and the cargo environment: the suite hands its request file back to
      # the invoking user, because a root-owned request is exactly what the helper refuses.
      - name: Elevated suite (Unix)
        if: runner.os != 'Windows'
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          set -euo pipefail
          suite=$(cargo test -p mixengine-elevate --test system --locked --offline --no-run \
            --message-format=json | jq -r 'select(.profile.test == true) | .executable' | tail -n 1)
          echo "running $suite as root"
          sudo -E "$suite" --ignored --nocapture --test-threads=1

      # T40a's launchers, which are in `mixengine-platform` rather than in the helper: what is under
      # test is the thing that *raises* the prompt. The helper is built explicitly rather than left to
      # the step above, because this suite finds it beside the test binary and a reordering of these
      # steps should not silently turn that into a skip.
      - name: Build the launcher suite
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          cargo build -p mixengine-elevate --locked --offline
          cargo test -p mixengine-platform --test elevation --locked --offline --no-run

      # As the elevated suite above: this leg already holds a full administrator token, so `runas`
      # raises nothing and the round trip runs unattended.
      - name: Launchers (Windows)
        if: runner.os == 'Windows'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-platform --test elevation --locked --offline -- --ignored --nocapture --test-threads=1

      # `timeout-minutes` of its own, rather than the job's 20. The macOS leg is a **measurement**:
      # whether `do shell script … with administrator privileges` authenticates on a process that is
      # already root is not something this project can settle by reading. If it prompts, it hangs, and
      # this is the clock that ends it — see the T40a design, "Testing". The Linux leg needs no token
      # at all and asserts the opposite: that a machine with no authentication agent says so.
      - name: Launchers (Unix)
        if: runner.os != 'Windows'
        timeout-minutes: 5
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          set -euo pipefail
          suite=$(cargo test -p mixengine-platform --test elevation --locked --offline --no-run \
            --message-format=json | jq -r 'select(.profile.test == true) | .executable' | tail -n 1)
          echo "running $suite as root"
          sudo -E "$suite" --ignored --nocapture --test-threads=1

      # T41's hosts engine, against the machine's own `/etc/hosts`. Its unit tests drive the same
      # `apply` against files they own; what only a real machine can answer is whether the replace
      # keeps the file's owner and its ACL, and whether an ordinary token is refused. The test takes
      # a copy first and asserts the file came back byte for byte, so a runner is left as it was.
      - name: Build the hosts suite
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-platform --test hosts --locked --offline --no-run

      - name: The hosts file (Windows)
        if: runner.os == 'Windows'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-platform --test hosts --locked --offline -- --ignored --nocapture --test-threads=1

      # `--test-threads=1` for the elevated suite's reason: there is one hosts file on this machine.
      - name: The hosts file (Unix)
        if: runner.os != 'Windows'
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          set -euo pipefail
          suite=$(cargo test -p mixengine-platform --test hosts --locked --offline --no-run \
            --message-format=json | jq -r 'select(.profile.test == true) | .executable' | tail -n 1)
          echo "running $suite as root"
          sudo -E "$suite" --ignored --nocapture --test-threads=1

      # T42's port access, against each machine's own mechanism. The unit tests drive the codec and
      # the pf text against strings; what only a real machine answers is whether the kernel
      # recognises the attribute this writes, whether it clears it on a write, and whether pf
      # actually sends 80 to a server on 8080. Both tests take a copy of whatever they touch and
      # assert the machine came back as it was.
      #
      # Windows has no leg: that system grants nothing, its whole answer is a refusal, and a refusal
      # needs no token — so it is asserted in the `test` job with everything else.
      - name: Build the port-access suite
        if: runner.os != 'Windows'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-platform --test port_access --locked --offline --no-run

      # `--test-threads=1` for the hosts suite's reason: there is one `/etc/pf.conf` on this machine
      # and one packet filter, and two tests interleaving on them would assert about each other's
      # half-finished work.
      - name: Port access (Unix)
        if: runner.os != 'Windows'
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          set -euo pipefail
          suite=$(cargo test -p mixengine-platform --test port_access --locked --offline --no-run \
            --message-format=json | jq -r 'select(.profile.test == true) | .executable' | tail -n 1)
          echo "running $suite as root"
          sudo -E "$suite" --ignored --nocapture --test-threads=1

      # T45's resolver wiring, against each machine's own mechanism. **A leg on all three**, unlike
      # port access: Windows has a real mechanism here rather than a refusal.
      #
      # Every test that concludes anything from a name that did not resolve first proves its own DNS
      # server is answering — the T45 design, D14. That rule exists because four of the six
      # measurement rounds behind this task were void: the fake server was started in one step and
      # asked from the next, the runner killed it with the step, and nothing noticed until a control
      # was added. Which is also why the suite is one test rather than several sharing a server.
      - name: Build the resolver suite
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-platform --test resolver --locked --offline --no-run

      - name: The resolver (Windows)
        if: runner.os == 'Windows'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-platform --test resolver --locked --offline -- --ignored --nocapture --test-threads=1

      # `--test-threads=1` for the hosts suite's reason: there is one resolver configuration on this
      # machine, and two tests interleaving on it would assert about each other's half-finished work.
      - name: The resolver (Unix)
        if: runner.os != 'Windows'
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          set -euo pipefail
          suite=$(cargo test -p mixengine-platform --test resolver --locked --offline --no-run             --message-format=json | jq -r 'select(.profile.test == true) | .executable' | tail -n 1)
          echo "running $suite as root"
          sudo -E "$suite" --ignored --nocapture --test-threads=1

      # T49a and T54's writes — the only tests in the workspace that touch this machine's own trust
      # store, and until now the only ones no job ever ran. `.claude/standards/testing.md` rule 1
      # gates them on `MIXENGINE_SYSTEM_TESTS=1` **as well as** `#[ignore]`, because `#[ignore]` alone
      # is not enough: the `test` job runs the `caddy` suite with `--ignored`, and without the second
      # gate a rotation would install and remove a certificate authority on every Windows and macOS
      # runner in it. Nothing set the variable anywhere, so what the second gate bought was that a
      # rotation had never been measured at all — while T52, T53 and T54 are ticked `[x]`.
      #
      # This is the job both designs name for them, and the job whose whole purpose is writing to the
      # machine. The Caddy comes down here rather than in the fetch block of `test` because this job
      # has no network namespace to get in ahead of; nothing below it is offline but cargo.
      - name: Fetch a real Caddy
        if: runner.os != 'Linux'
        env:
          CADDY: "2.11.4"
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS-$RUNNER_ARCH" in
            macOS-ARM64)   target=macos-aarch64;   ext=tar.zst ;;
            macOS-X64)     target=macos-x86_64;    ext=tar.zst ;;
            Windows-X64)   target=windows-x86_64;  ext=zip ;;
            Windows-ARM64) target=windows-aarch64; ext=zip ;;
            *) echo "::error::mixengine-packages publishes no Caddy for $RUNNER_OS-$RUNNER_ARCH"; exit 1 ;;
          esac

          into="$RUNNER_TEMP/caddy"
          archive="$RUNNER_TEMP/caddy.$ext"
          mkdir -p "$into"

          curl --fail --silent --show-error --location --retry 3 --output "$archive" \
            "https://github.com/mixnz/mixengine-packages/releases/download/caddy-$CADDY/caddy-$CADDY-$target.$ext"

          # The `test` job's own Caddy step says why the Windows arm names its `tar` outright.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$SYSTEMROOT/System32/tar.exe" -xf "$archive" -C "$into"
          else
            tar -xf "$archive" -C "$into" || zstd -dc "$archive" | tar -xf - -C "$into"
          fi

          echo "MIXENGINE_CADDY_PACKAGE=$into" >> "$GITHUB_ENV"
          "$into/caddy$( [ "$RUNNER_OS" = "Windows" ] && echo .exe )" version

      # Both suites drive a real `mix` against a real `mixengined`, and a rotation reaches the store
      # through the `mixengine-elevate` beside it — three binaries in one directory, which is what
      # `cargo test --workspace` hands the other jobs and what this one has to ask for. The helper is
      # named here as well as in the launcher step above, for that step's reason: a reordering should
      # not silently turn it into a skip.
      - name: Build the certificate suites
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          cargo build -p mixengine-daemon -p mixengine-elevate --locked --offline
          cargo test -p mixengine-cli --test cert --locked --offline --no-run

      - name: Build the certificate suite that needs a front end
        if: runner.os != 'Linux'
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test caddy --locked --offline --no-run

      # `--test-threads=1` for the hosts suite's reason, with the store in place of the file: there is
      # one trust store on this machine.
      #
      # **The whole `caddy` suite and not the one gated test in it.** A `--exact` filter would be
      # precise today and silent the day somebody renames the test — cargo answers a filter that
      # matched nothing with `0 tests` and exit 0, which is the reporting-a-pass this job exists to
      # avoid. The four front-end tests it re-runs are not waste: they ran unprivileged in `test`, and
      # here they run under a token that can actually write, which is a different machine.
      - name: The certificate authority this machine ends up trusting (Windows)
        if: runner.os == 'Windows'
        timeout-minutes: 12
        env:
          CARGO_NET_OFFLINE: "true"
          MIXENGINE_SYSTEM_TESTS: "1"
        run: |
          cargo test -p mixengine-cli --test cert --locked --offline -- --ignored --nocapture --test-threads=1
          cargo test -p mixengine-cli --test caddy --locked --offline -- --ignored --nocapture --test-threads=1

      # As root, in the elevated suite's shape, and **that is what makes this leg meaningful**: `do
      # shell script … with administrator privileges` on a process that is already root runs straight
      # through, which the launcher step above measured rather than assumed. So a rotation here is
      # granted, and the gated `caddy` test — which asserts `outcome == "rotated"` and then asks the
      # running server what it presents — has something to assert about.
      #
      # `timeout-minutes` of its own for the launcher step's reason: if a prompt does appear after
      # all, nothing on this runner can answer it, and this is the clock that ends the wait.
      - name: The certificate authority this machine ends up trusting (macOS)
        if: runner.os == 'macOS'
        timeout-minutes: 12
        env:
          CARGO_NET_OFFLINE: "true"
          MIXENGINE_SYSTEM_TESTS: "1"
        run: |
          set -euo pipefail
          for name in cert caddy; do
            suite=$(cargo test -p mixengine-cli --test "$name" --locked --offline --no-run \
              --message-format=json | jq -r 'select(.profile.test == true) | .executable' | tail -n 1)
            echo "running $suite as root"
            sudo -E "$suite" --ignored --nocapture --test-threads=1
          done

      # **`cert` and not `caddy`, and root changes nothing about that.** A runner has no polkit agent
      # — which is not a gap in this job but ADR 0005's worst branch, asserted for real by the
      # launcher step above — so `probe()` answers `Unavailable` whoever is asking and a rotation is
      # never granted here. The gated `caddy` test requires `outcome == "rotated"`, so on this system
      # it could only ever fail; the four front-end tests beside it already run in `test`, inside the
      # network namespace. What Linux does answer is the refusal, and that is exactly what the `cert`
      # test asserts: replaced or left exactly as it was, and in neither case a candidate private key
      # left on disk.
      - name: The certificate authority this machine ends up trusting (Linux)
        if: runner.os == 'Linux'
        timeout-minutes: 12
        env:
          CARGO_NET_OFFLINE: "true"
          MIXENGINE_SYSTEM_TESTS: "1"
        run: |
          set -euo pipefail
          suite=$(cargo test -p mixengine-cli --test cert --locked --offline --no-run \
            --message-format=json | jq -r 'select(.profile.test == true) | .executable' | tail -n 1)
          echo "running $suite as root"
          sudo -E "$suite" --ignored --nocapture --test-threads=1

      # The daemon's autostart entry — T85b. **This job and no other**, because these tests register a
      # real logon task and a real systemd user unit: `.claude/standards/testing.md` rule 1 gates them
      # on `MIXENGINE_SYSTEM_TESTS=1` as well as `#[ignore]`, and this is the job whose purpose is
      # writing to the machine.
      #
      # A scratch name on both systems — `MixEngineSystemSuite`, `mixengine-system-suite.service` —
      # never the entry a person's daemon depends on, and each test removes what it made. macOS runs
      # nothing extra here: that leg writes one file and drives no tool (the T85b design, D8a), so its
      # whole cycle is already proved unignored in the `test` job against a directory it owns.
      #
      # **The Linux leg asserts one of two things** and prints which: a runner with a systemd user
      # manager registers and reads back, and one without has to refuse with a reason naming the
      # command to run by hand. Neither branch passes by doing nothing.
      #
      # `--test-threads=1` for the hosts suite's reason with the login configuration in place of the
      # file: there is one of it on this machine. As the ordinary user, deliberately: every mechanism
      # here is per-user and nothing about it needs root.
      - name: The daemon's autostart entry
        if: runner.os != 'macOS'
        env:
          CARGO_NET_OFFLINE: "true"
          MIXENGINE_SYSTEM_TESTS: "1"
        run: cargo test -p mixengine-platform --lib --locked --offline autostart -- --ignored --nocapture --test-threads=1

      # What that suite left behind, on the resolver step's shape below and for its reason.
      - name: The autostart entries this job left behind
        if: always() && runner.os != 'macOS'
        shell: bash
        run: |
          case "${{ runner.os }}" in
            Windows) schtasks /Query /FO LIST | grep -i mixengine || echo "no MixEngine task, which is how a runner starts" ;;
            *)       ls -la ~/.config/systemd/user/ 2>/dev/null || echo "no user units, which is how a runner starts" ;;
          esac

      # T87. **The clean-VM smoke test the roadmap asks for, and a fresh runner is the clean VM**: it
      # has never had MixEngine on it, so what the suite finds on the machine after a grant is what
      # MixEngine put there and nothing else.
      #
      # **Last of the suites in this job, and that ordering is the whole of why it is here rather than
      # beside `cert`.** It removes the hosts block, the resolver wiring, the port grant, the
      # certificate authority, the privileged helper and the audit log; every suite above it would
      # then be running against a machine this one had already taken apart.
      #
      # Built as the ordinary user and run as root on Unix, which is this job's standing arrangement.
      # `--test-threads=1` for the hosts suite's reason: there is one of each of these on a machine.
      - name: Build the uninstall suite
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test -p mixengine-cli --test uninstall --locked --offline --no-run

      - name: Nothing of ours is left on this machine (Windows)
        if: runner.os == 'Windows'
        timeout-minutes: 12
        env:
          CARGO_NET_OFFLINE: "true"
          MIXENGINE_SYSTEM_TESTS: "1"
        run: cargo test -p mixengine-cli --test uninstall --locked --offline -- --ignored --nocapture --test-threads=1

      - name: Nothing of ours is left on this machine (Unix)
        if: runner.os != 'Windows'
        timeout-minutes: 12
        env:
          CARGO_NET_OFFLINE: "true"
          MIXENGINE_SYSTEM_TESTS: "1"
        run: |
          set -euo pipefail
          suite=$(cargo test -p mixengine-cli --test uninstall --locked --offline --no-run \
            --message-format=json | jq -r 'select(.profile.test == true) | .executable' | tail -n 1)
          echo "running $suite as root"
          sudo -E "$suite" --ignored --nocapture --test-threads=1

      # What the resolver suite left behind, so a failure has the evidence beside it — the packet
      # filter step's shape, for its reason. The suite puts the machine back; this is how we find out
      # on the run where it did not.
      - name: The resolver configuration this job left behind
        if: always()
        shell: bash
        run: |
          case "${{ runner.os }}" in
            Windows) reg query "HKLM\SYSTEM\CurrentControlSet\services\Dnscache\Parameters\DnsPolicyConfig" /s || echo "no policy rules, which is how a runner starts" ;;
            macOS)   ls -la /etc/resolver/ 2>/dev/null || echo "no /etc/resolver, which is how a runner starts" ;;
            *)       ls -la /etc/systemd/network/ 2>/dev/null || true ; resolvectl status 2>/dev/null | head -30 || echo "no systemd-resolved here" ;;
          esac

      # What the macOS leg left in the packet filter, so a failure has the evidence beside it. The
      # test puts pf back the way it found it; this is how we find out on the run where it did not.
      - name: The packet filter this job left behind
        if: always() && runner.os == 'macOS'
        run: |
          # `sudo`, because `/dev/pf` belongs to root — which is D9's whole point and the reason the
          # daemon compares three files instead of asking pf. Without it this step printed
          # "pfctl: /dev/pf: Permission denied" and no evidence at all.
          sudo /sbin/pfctl -s info || true
          sudo /sbin/pfctl -s nat || true
          echo "--- /etc/pf.conf ---"
          cat /etc/pf.conf
          ls -l /etc/pf.anchors/ /Library/LaunchDaemons/ | grep -i mixengine || echo "nothing of ours"

      # T49a's store, for the packet filter's reason and one more. This job's macOS leg is the only
      # place `security add-trusted-cert` and `remove-trusted-cert` are ever run, and the round trip
      # asks two different questions of them: whether the certificate is *in* the keychain, which is
      # what `TrustStore::probe` measures, and whether the machine actually *trusts* it, which is
      # what the operation is for. Those are not the same fact, and nothing before this printed
      # either of them — the first run of this suite hung for twenty minutes and produced no
      # evidence at all.
      - name: The trust store this job left behind
        if: always()
        shell: bash
        run: |
          case "${{ runner.os }}" in
            Windows) certutil -store Root | grep -i -A 2 mixengine || echo "nothing of ours in LocalMachine\Root" ;;
            macOS)
              # Bounded and given no console, for the reason this step exists: a `security` that
              # waits for an answer nobody can give is exactly what the suite ran into, and a
              # diagnostic that inherits the fault it was added to diagnose burns the job's twenty
              # minutes a second time. `alarm` survives `exec`, so the limit lands on `security`.
              bounded() { perl -e 'alarm 30; exec @ARGV' "$@" </dev/null; }
              echo "--- certificates in the System keychain ---"
              bounded security find-certificate -a -c "MixEngine" /Library/Keychains/System.keychain || echo "nothing of ours, or the read did not answer"
              echo "--- admin trust settings ---"
              bounded sudo security dump-trust-settings -d || echo "no admin trust settings, or the read did not answer"
              ;;
            *)
              ls -l /usr/local/share/ca-certificates/ /etc/pki/ca-trust/source/anchors/ 2>/dev/null | grep -i mixengine || echo "nothing of ours in the anchors directories"
              ;;
          esac

      # What the suite left behind, so a failure has the evidence beside it.
      # T85. The privileged helper this job installed, and where. `always()` for the reason every
      # step in this block has it: what an elevated job left on a machine is worth printing whether
      # or not the assertions about it held.
      - name: The privileged helper this job installed
        if: always()
        shell: bash
        run: |
          case "${{ runner.os }}" in
            # `ProgramFiles`, in the runner's own spelling, for the reason the audit log's step
            # below records about `ProgramData`: bash is case-sensitive about a name Windows is not.
            Windows) helper="${ProgramFiles:-C:/Program Files}/MixEngine/mixengine-elevate.exe" ;;
            macOS)   helper="/Library/PrivilegedHelperTools/dev.mixengine.elevate" ;;
            *)       helper="/usr/local/libexec/mixengine/mixengine-elevate" ;;
          esac
          if [ -e "$helper" ]; then ls -l "$helper"; else echo "no helper at $helper"; fi

      # T87. What the uninstall suite left, which on a passing run is nothing — and on a failing one
      # is the whole answer. `always()` for the reason every step in this block has it: what an
      # elevated job left on a machine is worth printing whether or not the assertions about it held.
      #
      # **Windows prints its pending-rename queue**, because there the helper cannot be unlinked while
      # it is running and what the suite asserts is that the operating system accepted the removal —
      # the T87 design, D8.
      - name: What the uninstall suite left behind
        if: always()
        shell: bash
        run: |
          case "${{ runner.os }}" in
            Windows)
              reg query "HKLM\SYSTEM\CurrentControlSet\Control\Session Manager" /v PendingFileRenameOperations || echo "nothing scheduled for the next restart"
              ls -l "${ProgramFiles:-C:/Program Files}/MixEngine" 2>/dev/null || echo "no MixEngine directory under Program Files, which is the answer"
              ;;
            macOS)
              ls -la /etc/resolver/ 2>/dev/null || echo "no /etc/resolver, which is the answer"
              ls -l /Library/PrivilegedHelperTools/ | grep -i mixengine || echo "no helper, which is the answer"
              ls -l /Library/Logs/MixEngine 2>/dev/null || echo "no audit log directory, which is the answer"
              ;;
            *)
              ls -l /usr/local/libexec/mixengine 2>/dev/null || echo "no helper directory, which is the answer"
              ls -l /var/log/mixengine 2>/dev/null || echo "no audit log directory, which is the answer"
              ;;
          esac
          echo "--- the managed hosts block ---"
          grep -c "BEGIN MixEngine" /etc/hosts 2>/dev/null || grep -c "BEGIN MixEngine" "${SystemRoot:-C:/Windows}/System32/drivers/etc/hosts" 2>/dev/null || echo "no block, which is the answer"

      - name: The audit log this job produced
        if: always()
        shell: bash
        run: |
          case "${{ runner.os }}" in
            # `ProgramData`, not `PROGRAMDATA`: Windows itself is case-insensitive about the name,
            # but bash is not, and the runner exports it in this spelling. The first run of this
            # step printed "no log at /MixEngine/elevate.log".
            Windows) log="${ProgramData:-C:/ProgramData}/MixEngine/elevate.log" ;;
            macOS)   log="/Library/Logs/MixEngine/elevate.log" ;;
            *)       log="/var/log/mixengine/elevate.log" ;;
          esac
          if [ -f "$log" ]; then cat "$log"; else echo "no log at $log"; fi

  # T29. The performance budgets from .claude/standards/testing.md, of which the shim's is the first
  # to exist. It is a job of its own rather than a step in `test` for two reasons: these are
  # `#[ignore]`d and would otherwise be skipped there anyway, and they need a **release** build,
  # which is a second compilation of the workspace that no correctness answer should have to wait
  # behind.
  #
  # On all three systems, unlike the single-runner row the ops document first sketched: the gate is
  # the same everywhere, but the hand-over it stands in front of is not one mechanism — Unix `exec`s
  # and Windows starts a child inside a Job Object — and the reported wall clock is the only place
  # that difference is ever written down as a number.
  bench:
    name: bench (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    # Ninety rather than the thirty this job started with: M3 added a release build of the
    # daemon and the client, three archives to fetch, and a suite that starts three real servers
    # eight times; T72a added three PHP archives and a suite that waits out a sweep before it
    # can measure anything; and T73 added one that bootstraps two databases and settles each.
    timeout-minutes: 90
    strategy:
      # A budget missed on one system is not a reason to stop measuring the other two: the numbers
      # are only comparable when they were all taken.
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install pinned toolchain
        run: |
          rustup toolchain install --no-self-update
          rustup show active-toolchain

      - uses: Swatinem/rust-cache@v2
        with:
          key: bench

      - name: Fetch dependencies
        run: cargo fetch --locked

      # **M3's three servers**, fetched before the offline steps below because from there on there is
      # no network. One script rather than three more copies of the `test` job's fetch steps — see
      # the note at the top of `.github/scripts/fetch-package.sh`.
      #
      # The versions are the ones the `test` job's own steps pin, and they are pinned in two places
      # deliberately: a bench comparing itself against last month's number has to be measuring the
      # same programs, and a suite bumped by somebody else's step would move the number without
      # anybody deciding to.
      - name: Fetch the three servers M3 is about
        shell: bash
        run: |
          set -euo pipefail
          bash .github/scripts/fetch-package.sh caddy 2.11.4 MIXENGINE_CADDY_PACKAGE
          bash .github/scripts/fetch-package.sh mariadb 11.4.12 MIXENGINE_MARIADB_PACKAGE
          bash .github/scripts/fetch-package.sh redis 8.10.0 MIXENGINE_REDIS_PACKAGE

      # **Three PHPs, and three different versions on purpose** — roadmap task T72a. The cold path is
      # measured three times because one CI measurement has misled this project before: the M3 bench
      # is bimodal on ubuntu, where a red has meant a bad minute rather than a regression. Three
      # rounds need three *cold* pools, and a pool is only cold once — so three pools, which means
      # three runtime installs, which means three versions.
      #
      # 7.0.33 is the floor this product offers, 7.4.33 is the legacy version people still run, and
      # 8.3.33 is what the `test` job pins. **The first two predate `pm.status_listen` entirely**, so
      # this is also the standing proof of T72a's decision to read a pool's status page off its own
      # socket: the day somebody reaches for the cleaner arithmetic, two thirds of this measurement
      # go red rather than a paragraph being disbelieved.
      #
      # A directory each, because the script's default would have them overwrite one another, and
      # `MIXENGINE_PHP_RUNTIMES` is the list the suite reads — separated the way this system
      # separates a PATH.
      - name: Fetch the three PHPs the cold path wakes
        shell: bash
        run: |
          set -euo pipefail
          bash .github/scripts/fetch-package.sh php 7.0.33 MIXENGINE_PHP_70 "$RUNNER_TEMP/php-70"
          bash .github/scripts/fetch-package.sh php 7.4.33 MIXENGINE_PHP_74 "$RUNNER_TEMP/php-74"
          bash .github/scripts/fetch-package.sh php 8.3.33 MIXENGINE_PHP_83 "$RUNNER_TEMP/php-83"

          if [ "$RUNNER_OS" = "Windows" ]; then separator=';'; else separator=':'; fi
          echo "MIXENGINE_PHP_RUNTIMES=$RUNNER_TEMP/php-70$separator$RUNNER_TEMP/php-74$separator$RUNNER_TEMP/php-83" >> "$GITHUB_ENV"

      # T33's requirement, seen from the job that is not `test`: the MariaDB bootstrap puts the
      # generated root password in the OS credential store and refuses a machine with none. Windows
      # and macOS have one in the OS. On Linux this installs it, and the measuring step below wraps
      # itself in a session bus of its own — the smaller half of what
      # `.github/scripts/test-no-network.sh` does, rather than teaching that script a second profile.
      #
      # The network namespace that script also sets up is deliberately not reproduced: this suite
      # talks to a `MockRegistry` on loopback like every other, and the isolation the `test` job adds
      # is a belt the `bench` job has never worn.
      - name: Install a secret service
        if: runner.os == 'Linux'
        timeout-minutes: 6
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          set -euo pipefail

          for attempt in 1 2 3; do
            sudo apt-get -o Acquire::Retries=3 -o Acquire::http::Timeout=20 -o Acquire::https::Timeout=20 update && break
            echo "::warning title=apt-get update failed::attempt $attempt of 3"
            sleep 5
          done

          sudo apt-get install -y --no-install-recommends gnome-keyring dbus

      # `fakeservice` is the program the shim is measured in front of, and it is a **binary of a
      # dev-dependency crate**: selecting one test target does not build it, and a release copy left
      # by an earlier build is used as it is. A stale one is what this step exists for — it was
      # found by the benchmark's own check that the fronted program really ran, having been built
      # before that program learned `--version`, and a benchmark that measures a binary nobody
      # rebuilt is a benchmark measuring last month.
      - name: Build the program the shim is measured against
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo build --release -p mixengine-testkit --bin fakeservice --locked --offline

      # And the daemon the M3 suite drives, for the same reason and a different one: `mixengined` is
      # another package, so `cargo test -p mixengine-cli` never builds it — the suite finds it beside
      # the `mix` it was given, which is exactly how a released `mix` finds it at run time.
      - name: Build the daemon M3 measures
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo build --release -p mixengine-daemon --locked --offline

      # `--test-threads=1` is load-bearing rather than tidiness: both tests spend their whole time
      # creating processes, so run in parallel each of them measures the other — 34 ms against 21 ms
      # for the same difference on the same machine. `--nocapture` because the numbers are the
      # output, and a passing test's stdout is otherwise swallowed.
      - name: Performance budgets
        env:
          CARGO_NET_OFFLINE: "true"
        run: >
          cargo test --release -p mixengine-shim --test overhead --locked --offline
          -- --ignored --nocapture --test-threads=1

      # **The idle footprint**, and it runs *before* M3 deliberately — `features/resource-isolation.md`'s first published number, and the
      # third thing this job gates. Its own step rather than another `--test` on a line above, on
      # this job's rule: a failure should name what failed without anybody reading the log.
      #
      # It needs the Caddy fetched above and nothing else — one daemon, one web server, and nothing
      # running. It spends thirty seconds settling before the first reading, deliberately: what is
      # measured is a home doing nothing, and a reading taken the instant a start walk returned would
      # measure the walk.
      #
      # **No `dbus-run-session` wrapper**, unlike the M3 steps: nothing here starts a MariaDB, so
      # nothing here has a password to put in a credential store.
      #
      # **Ahead of M3 because a failing step ends the job.** This one takes forty seconds and
      # needs one server; M3 starts three of them eight times over and is bimodal on ubuntu. The
      # first run of this budget was skipped on that runner for exactly that reason, which is a
      # measurement lost to somebody else's bad minute.
      - name: Idle footprint
        timeout-minutes: 10
        env:
          CARGO_NET_OFFLINE: "true"
        run: >
          cargo test --release -p mixengine-cli --test idle_footprint --locked --offline
          -- --ignored --nocapture

      # **The cold path**, `features/resource-isolation.md`'s second published number and the fourth
      # thing this job gates — roadmap task T72a. Its own step for this job's rule: a failure should
      # name what failed without anybody reading the log.
      #
      # **Ahead of M3 for the idle footprint's reason**, and it needs the reason more: a failing step
      # ends the job, and this one waits a full minute for the sweeper before it can measure
      # anything. Losing that to somebody else's bimodal warm start would cost the whole
      # measurement.
      #
      # It needs the Caddy and the three PHPs fetched above and nothing else — no MariaDB, so no
      # `dbus-run-session` wrapper and no credential store on Linux.
      #
      # **What it waits for is a sweep, not a clock.** The pools must be stopped by the sweeper and
      # not by a person, because a service somebody stopped is one the activator deliberately refuses
      # to wake (T70's D8). An idle policy is a whole number of minutes, so the wait is about ninety
      # seconds and is paid once for all three rounds.
      - name: Cold path
        timeout-minutes: 15
        env:
          CARGO_NET_OFFLINE: "true"
        run: >
          cargo test --release -p mixengine-cli --test cold_path --locked --offline
          -- --ignored --nocapture

      # **What the tuned templates save** — roadmap task T73, and the fifth thing this job measures.
      # Two MariaDB instances in one home, one on the recipe's defaults and one put back to the
      # server's own, started in turn and read through the daemon's own sampler.
      #
      # **What is gated is the difference between them**, never either number on its own: an
      # absolute budget on MariaDB's RSS would be a promise held hostage to next month's MariaDB, on
      # a quantity this project does not control. The suite's module note argues it in full.
      #
      # **Ahead of M3 for the idle footprint's reason** — a failing step ends the job, and M3 is
      # bimodal on ubuntu, where a red has meant a bad minute rather than a regression. Behind the
      # cold path because this one bootstraps two databases and that one waits out a sweep; neither
      # should be paying for the other's bad luck.
      #
      # It needs the MariaDB fetched above, and a credential store with it: the bootstrap generates a
      # root password and refuses a machine that cannot hold one. Windows and macOS have one in the
      # OS; Linux gets the same `dbus-run-session` wrapper the M3 step below uses.
      - name: What the tuned defaults save
        if: runner.os != 'Linux'
        timeout-minutes: 20
        env:
          CARGO_NET_OFFLINE: "true"
        run: >
          cargo test --release -p mixengine-cli --test tuned_footprint --locked --offline
          -- --ignored --nocapture

      - name: What the tuned defaults save
        if: runner.os == 'Linux'
        timeout-minutes: 20
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          set -euo pipefail

          dbus-run-session -- sh -c \
            'printf "mixengine-ci" | gnome-keyring-daemon --unlock --components=secrets >/dev/null || exit 1
             exec cargo test --release -p mixengine-cli --test tuned_footprint --locked --offline -- --ignored --nocapture'

      # **M3**, which is `.claude/features/services.md`'s one number about starting things: caddy,
      # mariadb and redis healthy in under ten seconds warm. Its own step rather than another
      # `--test` on the line above, for the reason every real-server step in the `test` job has one:
      # a failure should name what failed without anybody reading the log.
      #
      # `--nocapture` because the numbers are the output. A timeout of its own because this starts
      # three servers eight times over and a hang inside libtest prints nothing until it ends — the
      # suite carries a watchdog of `mariadb.rs`' kind for that, and this is the outer bound.
      #
      # Windows and macOS run it directly; Linux wraps it in a session bus with a `gnome-keyring` on
      # it, because the MariaDB bootstrap has a password to store and refuses a machine that cannot.
      - name: M3 — three services, warm
        if: runner.os != 'Linux'
        timeout-minutes: 20
        env:
          CARGO_NET_OFFLINE: "true"
        run: >
          cargo test --release -p mixengine-cli --test warm_start --locked --offline
          -- --ignored --nocapture

      - name: M3 — three services, warm
        if: runner.os == 'Linux'
        timeout-minutes: 20
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          set -euo pipefail

          dbus-run-session -- sh -c \
            'printf "mixengine-ci" | gnome-keyring-daemon --unlock --components=secrets >/dev/null || exit 1
             exec cargo test --release -p mixengine-cli --test warm_start --locked --offline -- --ignored --nocapture'

  # T56, and the fifth job of the table in .claude/operations/build-and-release.md: the published
  # TypeScript contract, regenerated from `mixengine-proto` and compared with what is committed.
  # Until this existed, a `ts-rs` type whose committed output had drifted was caught by a person or
  # by nobody.
  #
  # **Ubuntu alone.** Generation is OS-independent — measured, and the `test` job runs the generator
  # on all three every run, because it passes `--all-features` and the exporter is a `#[test]`. So a
  # second and third runner here would re-measure one behaviour at twice the cost, which is the
  # reasoning T86a's `windows-latest` probe uses one job along.
  bindings:
    name: bindings
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install pinned toolchain
        run: |
          rustup toolchain install --no-self-update
          rustup show active-toolchain

      - uses: Swatinem/rust-cache@v2
        with:
          key: bindings

      - name: Fetch dependencies
        run: cargo fetch --locked

      # The gate. It regenerates into a temporary directory and diffs, so a red job is a message and
      # not also a dirty checkout — and so it answers the same way on a machine that unpacked a
      # tarball rather than cloned one.
      - name: The committed contract is what mixengine-proto generates
        run: bash packaging/bindings.sh --check

      # Redundant with `lint`, which passes `--all-features`, and kept: this is the job somebody
      # opens when the contract is the thing that broke, and the crate is already compiled here.
      - name: The contract's own feature is clean
        run: cargo clippy -p mixengine-proto --features ts --all-targets --locked -- -D warnings

      # So the packing path runs on every CI run rather than for the first time during a release —
      # the rule `lint` already applies to `sign.sh` and `feed.sh`, whose only other caller is also
      # a release. The script opens the archive it just made and asserts what is in it.
      - name: The archive a release publishes can be built
        run: bash packaging/bindings.sh --pack

  # T90: the user handbook. The corpus's own invariants — parity between the two languages,
  # resolvable links, a translation that was revisited after its source changed, prose that is
  # wrapped — are `cargo test` and therefore run in `test` on all three runners with no step here.
  # That is T89's rule: a suite that needs nothing downloaded and no privilege arrives without a job.
  #
  # What needs a job is the part that is not a test: building the site, which compiles a Markdown
  # renderer, and holding the committed command reference against what `mix` prints — the same shape
  # as `bindings`, for the same reason. A red job here names which of the two broke.
  #
  # **Ubuntu alone**, again for `bindings`' reason: the generator reads embedded strings and writes
  # files, and a second and third runner would re-measure one behaviour at twice the cost.
  docs:
    name: docs
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install pinned toolchain
        run: |
          rustup toolchain install --no-self-update
          rustup show active-toolchain

      - uses: Swatinem/rust-cache@v2
        with:
          key: docs

      - name: Fetch dependencies
        run: cargo fetch --locked

      - name: The corpus builds a site, and the committed command reference is current
        run: bash packaging/docs.sh --check

  # T85, and the sixth job of the table in .claude/operations/build-and-release.md: the artifacts a
  # person downloads. Three runners and host architecture only — the second architecture on Windows
  # and Linux is a cross-compilation of a workspace that builds SQLite, AWS-LC and libdbus from C on
  # runners that carry no cross toolchain, and it is roadmap task T85a rather than a flag added here.
  # macOS is the exception and is universal, because Apple's toolchain builds the other slice with no
  # extra sysroot.
  #
  # **Nothing here is signed.** Authenticode and an Apple Developer ID are not purchased (ADR 0005),
  # and the minisign signature that actually protects an update is T86's. Each script writes a
  # `.sha256` beside its artifact, which is not the same thing and is not offered as one.
  #
  # Each script also opens what it just made and checks the three binaries are in it, which is where
  # this job's real assertion lives: an empty archive is a perfectly valid archive, and a `.deb` with
  # no helper in it installs cleanly and leaves the machine one file short of being able to elevate.
  # T103, ADR 0027: the desktop application under apps/desktop. Its frontend's build, tests and
  # lint, then its own Cargo workspace's clippy and tests — on one OS, because nearly all of it is
  # pure logic (MixDB's own CI made the same choice), and `build` is what proves the window links
  # on all three. It gates `release` the way `bindings` does, and for the same reason: a type
  # reshaped in `mixengine-proto` fails `tsc` here, in the same run — the check ADR 0011 gave up.
  #
  # `cargo audit` is here rather than on a schedule of its own: this workflow only runs when
  # somebody asks, so an advisory published overnight turns red at the moment somebody is looking.
  # Its documented ignores are `apps/desktop/src-tauri/.cargo/audit.toml`'s.
  desktop:
    name: desktop
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: apps/desktop/.nvmrc
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Frontend — install, typecheck and build, test, lint
        working-directory: apps/desktop
        run: |
          npm ci
          npm run build
          npm test
          npm run lint

      # The Tauri crates link against webkit2gtk, so even `cargo check` wants it present.
      - name: Install the libraries the window links against
        run: |
          sudo apt-get update
          sudo apt-get install --yes libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libxdo-dev libssl-dev build-essential

      # The root's rust-toolchain.toml is the nearest pin walking up from apps/desktop.
      - name: Install pinned toolchain
        run: |
          rustup toolchain install --no-self-update
          rustup show active-toolchain

      - uses: Swatinem/rust-cache@v2
        with:
          key: desktop
          workspaces: "apps/desktop/src-tauri -> target"

      - name: clippy
        working-directory: apps/desktop/src-tauri
        run: cargo clippy --locked --all-targets -- -D warnings

      - name: test
        working-directory: apps/desktop/src-tauri
        run: cargo test --locked

      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit

      - name: Advisories against the window's dependency tree
        working-directory: apps/desktop/src-tauri
        run: cargo audit

  build:
    name: build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    # A release build of the whole workspace, and twice over on macOS for the two slices, and again
    # from scratch inside a container for both Linux legs — T85a, D3: nothing in this job's cache
    # reaches a location inside a container's bind mount, so those two legs pay for a cold `cargo
    # build --release` on every run. Accepted there rather than solved, until it is measured to cost
    # more than this timeout allows. T103 added a Tauri release build of the window to every leg,
    # which is roughly ten more minutes; the timeout moved from 60 to 75 for it.
    timeout-minutes: 75
    strategy:
      # An artifact that failed to build on one system is not a reason to stop producing the other
      # four — the same rule `test` and `bench` state one job up.
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            container: ""
          # T85a: native, not cross. GitHub's own arm64-hosted runners exist and are free for this
          # (public) repository, so this leg is the same shape as the row above rather than a
          # cross-compilation of it — see the design's "Measured, not assumed".
          - os: windows-11-arm
            target: aarch64-pc-windows-msvc
            container: ""
          - os: macos-latest
            target: ""
            container: ""
          # T85a, D2: moved into a manylinux_2_28 container so the artifact links an older glibc than
          # this runner ships, rather than whatever `ubuntu-latest` happens to carry this month. Same
          # architecture as the runner in both rows below — never cross-compilation, only an older
          # sysroot. The tag is pinned and dated for the reason every other toolchain in this product
          # is: "whatever the runner has" is not reproducible.
          #
          # T103: the window is built on the host of these two legs, not in the container — the
          # manylinux image is AlmaLinux 8, whose WebKitGTK is the 4.0 API on libsoup 2, and Tauri 2
          # links 4.1 on libsoup 3. The container step does not care what its host runs, so the host
          # is pinned to 22.04 to give the window the glibc 2.35 floor MixDB's own releases had.
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            container: quay.io/pypa/manylinux_2_28_x86_64:2026.09.02-0
          - os: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
            container: quay.io/pypa/manylinux_2_28_aarch64:2026.09.02-0
    steps:
      - uses: actions/checkout@v4

      # Only the legs that build on the runner itself rather than inside a container: the two Linux
      # legs install their own pinned toolchain inside `stage.sh --container`'s `docker run`, since
      # nothing installed here is visible from there.
      - name: Install pinned toolchain
        if: matrix.container == ''
        run: |
          rustup toolchain install --no-self-update
          rustup show active-toolchain

      - uses: Swatinem/rust-cache@v2
        if: matrix.container == ''
        with:
          key: build
          workspaces: |
            . -> target
            apps/desktop/src-tauri -> target

      - name: Fetch dependencies
        if: matrix.container == ''
        run: cargo fetch --locked

      # T103: the desktop application, on every leg, on the runner itself.
      - uses: actions/setup-node@v4
        with:
          node-version-file: apps/desktop/.nvmrc
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Install the libraries the window links against
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install --yes libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libxdo-dev libssl-dev build-essential

      # The two Linux legs install Rust inside the container for the four binaries; the window is
      # built out here and needs a toolchain of its own.
      - name: Install pinned toolchain for the window
        if: matrix.container != ''
        run: |
          rustup toolchain install --no-self-update
          rustup show active-toolchain

      - name: The window's second slice (macOS is universal)
        if: runner.os == 'macOS'
        run: rustup target add x86_64-apple-darwin aarch64-apple-darwin

      # `tauri build` runs `npm run build` itself (tauri.conf.json's beforeBuildCommand). No
      # bundle: packaging is `packaging/`'s and arrives with T105; macOS is the exception because
      # a webview application there is a `.app` directory, and that is what T105 will stage.
      - name: Build the window
        shell: bash
        working-directory: apps/desktop
        env:
          MIX_TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          npm ci
          if [ "$RUNNER_OS" = macOS ]; then
            npm run tauri -- build --bundles app --target universal-apple-darwin
          else
            npm run tauri -- build --no-bundle --target "$MIX_TARGET"
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: desktop-${{ matrix.os }}
          path: |
            apps/desktop/src-tauri/target/*/release/mixdb
            apps/desktop/src-tauri/target/*/release/mixdb.exe
            apps/desktop/src-tauri/target/universal-apple-darwin/release/bundle/macos/*.app
          if-no-files-found: error
          retention-days: 14

      # Neither is on the ubuntu image. Installed rather than made optional: an artifact that is
      # quietly not built is a release that is quietly missing one.
      #
      # `desktop-file-utils` is appimagetool's, and it is a hard requirement rather than a nicety —
      # measured on a machine without it, where the tool exits with "desktop-file-validate command is
      # missing" before it looks at the AppDir at all.
      - name: Install the packaging tools this runner is missing
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install --yes rpm desktop-file-utils

      # **NSIS is not on the windows image**, measured: the first run of this job got through the
      # release build and the portable zip and then stopped at "missing tools: makensis". `7z` and
      # `unzip` *are* there, which is why only this one is installed.
      #
      # Chocolatey puts it at `C:\Program Files (x86)\NSIS`, which is the path
      # `packaging/windows/build.sh` looks at first.
      - name: Install the packaging tools this runner is missing
        if: runner.os == 'Windows'
        shell: pwsh
        run: choco install nsis --no-progress --yes

      # T85a, D8: a minimal build fails in under a minute with a plain compiler error, rather than the
      # first sign of a broken toolchain or a missing container package being sixty minutes into a
      # full packaging run that then also exercises makensis/dpkg-deb/rpmbuild/appimagetool for
      # nothing. Only the two new legs — the other three have been proven by every run of this job
      # since T85.
      - name: Smoke test this leg's toolchain
        if: matrix.os == 'windows-11-arm' || matrix.os == 'ubuntu-24.04-arm'
        shell: bash
        env:
          MIX_TARGET: ${{ matrix.target }}
          MIX_CONTAINER: ${{ matrix.container }}
        run: |
          set -euo pipefail
          source packaging/common.sh
          if [ -n "$MIX_CONTAINER" ]; then
            mix_in_container "$MIX_CONTAINER" \
              "rustup target add '$MIX_TARGET' && cargo build --release --locked --target '$MIX_TARGET' -p mixengine-cli"
            "target/$MIX_TARGET/release/mix" --version
          else
            cargo build --release --locked --target "$MIX_TARGET" -p mixengine-cli
            "target/$MIX_TARGET/release/mix.exe" --version
          fi

      - name: Windows artifacts
        if: runner.os == 'Windows'
        shell: bash
        env:
          MIX_TARGET: ${{ matrix.target }}
        run: bash packaging/windows/build.sh

      - name: macOS artifacts
        if: runner.os == 'macOS'
        shell: bash
        run: bash packaging/macos/build.sh

      # T86a. What an unsigned release looks like to the machines that judge it, measured on the
      # artifacts this leg just built: the Mark-of-the-Web and quarantine attributes SmartScreen and
      # Gatekeeper actually read. Neither dialog can be seen from here — those two readings are a
      # person's, on the draft release, and are release-checklist item 4.
      #
      # **`windows-latest` alone.** Mark-of-the-Web is not architecture-dependent, so the arm64 leg
      # would re-measure one behaviour on a different file at twice the runner cost — the design, D12.
      #
      # `MIX_PROBE_INSTALL` is set here and in no other place. The readings behind it install for
      # real: the NSIS installer into a temporary directory and this account's `PATH`, and on macOS
      # the `.pkg` into `/usr/local/bin` and `/Library/PrivilegedHelperTools` as root, because there
      # is no `-target` that isolates that. Both probes put the machine back as they found it, and
      # the macOS one refuses outright if MixEngine is already installed on it.
      #
      # Each probe *fails* on a statement about our own artifact that came back wrong — an installer
      # that started propagating a mark, a package that acquired a signature nobody bought — and
      # records a *void reading* for anything this machine could not answer. So a red leg here is a
      # change to the release story, and a green one that measured nothing says so in its report.
      - name: What an unsigned release shows SmartScreen
        if: matrix.os == 'windows-latest'
        shell: bash
        env:
          MIX_TARGET: ${{ matrix.target }}
          MIX_PROBE_INSTALL: "1"
        run: bash packaging/windows/probe.sh

      - name: What an unsigned release shows Gatekeeper
        if: runner.os == 'macOS'
        shell: bash
        env:
          MIX_PROBE_INSTALL: "1"
        run: bash packaging/macos/probe.sh

      - name: Linux artifacts
        if: runner.os == 'Linux'
        shell: bash
        env:
          MIX_TARGET: ${{ matrix.target }}
          MIX_CONTAINER: ${{ matrix.container }}
        run: |
          set -euo pipefail
          bash packaging/linux/build-deb.sh
          bash packaging/linux/build-rpm.sh
          bash packaging/linux/build-appimage.sh
          bash packaging/linux/build-tarball.sh

      # For a person reading the log rather than for the gate: every script has already opened what
      # it made and refused if a binary was missing from it.
      - name: What this leg produced
        shell: bash
        run: ls -l target/packaging/dist

      - uses: actions/upload-artifact@v4
        with:
          name: mixengine-${{ matrix.os }}
          path: target/packaging/dist/*
          if-no-files-found: error
          retention-days: 14

  # T86. Three questions that need no compiler, so a release that cannot work fails in thirty seconds
  # instead of in an hour. It gates `release` and nothing else — a conditional `needs` is not a thing
  # Actions has, and making `build` wait behind this would slow every ordinary run for a job that has
  # nothing to say on one.
  preflight:
    name: preflight
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: The tag, the version, the key and the secrets agree
        env:
          UPDATE_SECRET_KEY: ${{ secrets.UPDATE_SECRET_KEY }}
          UPDATE_PASSWORD: ${{ secrets.UPDATE_PASSWORD }}
        run: |
          source packaging/common.sh
          version="$(mix_version)"
          test "$GITHUB_REF_NAME" = "v$version" || {
            echo "the tag is $GITHUB_REF_NAME and the workspace version is $version" >&2
            exit 1
          }

          pinned="$(sed -n 's/^pub const PUBLIC_KEY: &str = "\(.*\)";$/\1/p' \
            crates/mixengine-core/src/updates.rs)"
          committed="$(sed -n '2p' packaging/updates.pub | tr -d '\r')"
          test -n "$pinned" || {
            echo "no PUBLIC_KEY in crates/mixengine-core/src/updates.rs" >&2
            exit 1
          }
          test "$pinned" = "$committed" || {
            echo "packaging/updates.pub is not the key this build pins" >&2
            exit 1
          }

          test -n "$UPDATE_SECRET_KEY" || { echo "the UPDATE_SECRET_KEY secret is not set" >&2; exit 1; }
          test -n "$UPDATE_PASSWORD" || { echo "the UPDATE_PASSWORD secret is not set" >&2; exit 1; }

          echo "v$version, to be signed by $pinned"

  # T86, D4. Signing happens once, on one runner, and this is it. The five build legs are untouched:
  # the secret would otherwise reach five jobs, and `minisign` has no official build for the arm64
  # Windows runner.
  #
  # `needs` deliberately omits `system` and `bench`. Both run on the tag and a person reads them, but
  # neither gates: `bench` is bimodal on ubuntu, and a release blocked by somebody else's bad minute
  # is a release process nobody trusts. What gates is "the repository is consistent", "the code is
  # correct on three operating systems", and "the artifacts were built".
  release:
    name: release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [preflight, lint, test, bindings, desktop, build]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Install minisign
        run: |
          sudo apt-get update
          sudo apt-get install --yes minisign

      - uses: actions/download-artifact@v4
        with:
          path: legs

      # Five uploads into one directory. `merge-multiple` would let a name that appears twice be one
      # artifact silently replacing another, and "silently" is the word that rules it out.
      - name: Gather what the five legs built
        run: |
          set -euo pipefail
          dist="target/packaging/dist"
          mkdir -p "$dist"
          for file in legs/*/*; do
            name="$(basename "$file")"
            if [ -e "$dist/$name" ]; then
              echo "two build legs produced $name" >&2
              exit 1
            fi
            mv "$file" "$dist/$name"
          done
          ls -l "$dist"

      # T56. The API contract, packed from the committed tree — which is current because this job
      # needs `bindings`, rather than because a second generation happened here. Before the signing
      # step, like the feed below, because being in this directory is how a file gets signed;
      # `feed.sh` does not pick it up, since it matches a payload by the
      # `mixengine-<version>-<os>-…` shape and a helper by `mixengine-elevate-<version>-…`, and this
      # is neither.
      - name: Pack the published API contract
        run: bash packaging/bindings.sh --pack

      # T88. The feed lists the payload archives of every leg, and no build leg can see the other
      # four — so it is written here, and it is written *before* the step below, which is what puts
      # `latest.json.minisig` beside it under the name an installed MixEngine appends.
      - name: Write the update feed
        run: bash packaging/feed.sh --tag "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY"

      - name: Sign every artifact
        env:
          MIX_SIGN_SECRET_KEY: ${{ secrets.UPDATE_SECRET_KEY }}
          MIX_SIGN_PASSWORD: ${{ secrets.UPDATE_PASSWORD }}
        run: bash packaging/sign.sh

      # **A draft, and a person publishes it.** T88's feed lives at `releases/latest/download/`, and
      # that URL must not move because somebody pushed a tag to see what would happen; T86a has to
      # watch a real download's SmartScreen behaviour and needs a moment to be ready for it; and
      # publishing to the world is not the same deliberate act as tagging.
      #
      # Notes are generated from the commits and are a starting point: the draft exists to be edited.
      # The unsigned-binary warning is prepended ahead of the generated list rather than left to
      # CHANGELOG.md, because a release page is what a downloader actually reads before running
      # SmartScreen or Gatekeeper past its warning.
      - name: Assemble the draft release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          if ! gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            notes_header="$(printf '%s\n' \
              '### Installation notes' \
              '' \
              "Nothing is code-signed or notarised, by design: expect SmartScreen on Windows and Gatekeeper's" \
              '"Open Anyway" on macOS. A machine with Smart App Control enforcing is not supported. On ARM64' \
              'Windows some runtimes have no build of their own and run under emulation, marked `emulated` in' \
              '`mix runtime available`.')"
            gh release create "$GITHUB_REF_NAME" \
              --draft --title "$GITHUB_REF_NAME" --generate-notes \
              --notes "$notes_header"
          fi
          gh release upload "$GITHUB_REF_NAME" target/packaging/dist/* --clobber

      # T79a's D5, one repository along: *created* and *published* are different claims, and an
      # upload that clobbered the wrong asset is exactly what the second check catches.
      - name: Verify what was published, and not only what was signed
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p published
          gh release download "$GITHUB_REF_NAME" --dir published --pattern '*'
          pubkey="$(sed -n '2p' packaging/updates.pub | tr -d '\r')"
          checked=0
          for signature in published/*.minisig; do
            artifact="${signature%.minisig}"
            test -f "$artifact" || {
              echo "$signature was published with no artifact beside it" >&2
              exit 1
            }
            minisign -V -H -P "$pubkey" -m "$artifact" -q
            checked=$((checked + 1))
          done
          test "$checked" -gt 0 || {
            echo "the release carries no signatures" >&2
            exit 1
          }
          echo "$checked published artifacts verify against $pubkey"

      - name: Where it is
        run: |
          echo "A draft release is waiting at $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/releases" \
            "— review it and publish it by hand." >>"$GITHUB_STEP_SUMMARY"
