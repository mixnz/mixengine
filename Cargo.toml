[workspace]
resolver = "3"
members = [
    "crates/mixengine-proto",
    "crates/mixengine-platform",
    "crates/mixengine-supervisor",
    "crates/mixengine-core",
    "crates/mixengine-elevate",
    "crates/mixengine-daemon",
    "crates/mixengine-docs",
    "crates/mixengine-cli",
    "crates/mixengine-shim",
    "crates/mixengine-testkit",
]

[workspace.package]
version = "0.0.1-beta.1"
edition = "2024"
rust-version = "1.97.1"
# Dual licensed the way the Rust ecosystem conventionally is: the user picks either one.
license = "MIT OR Apache-2.0"
repository = "https://github.com/mixnz/mixengine"
publish = false

# Dependency direction is strictly downward and is enforced by the test in
# `crates/mixengine-proto/tests/workspace_layering.rs`:
#
#   proto       -> (nothing)
#   docs        -> (nothing)
#   platform    -> proto
#   supervisor  -> platform, proto
#   core        -> platform, proto
#   elevate     -> platform, proto
#   daemon      -> core, supervisor, platform, proto
#   cli         -> docs, platform, proto
#   shim        -> core, platform, proto
#
# `mixengine-testkit` sits outside that graph on purpose: it is a **dev-dependency only**, which the
# same test enforces separately, so nothing it contains can reach a shipped binary.
[workspace.dependencies]
mixengine-core = { path = "crates/mixengine-core" }
# The handbook, embedded at compile time — roadmap task T90. It depends on nothing at all: its front
# matter is parsed by a build script, so what `mix` links is a table of `&'static str`.
mixengine-docs = { path = "crates/mixengine-docs" }
# `default-features = false` here rather than in the one crate that wants a lean slice: cargo refuses
# a member's `default-features = false` on an inherited dependency whose workspace entry leaves the
# defaults on. So the default is off at the root and every dependent that wants the whole crate says
# `features = ["default"]` — which is all of them but `mixengine-elevate`, the binary that runs as
# root and takes `["elevated"]` alone. See the T40 design, D8.
mixengine-platform = { path = "crates/mixengine-platform", default-features = false }
mixengine-proto = { path = "crates/mixengine-proto" }
mixengine-supervisor = { path = "crates/mixengine-supervisor" }
mixengine-testkit = { path = "crates/mixengine-testkit" }

anyhow = "1.0.104"
clap = { version = "4.6.6", features = ["derive", "env"] }
# `RequestHandler`, and nothing else. `hickory-server` declares that trait with `#[async_trait]`
# rather than a native `async fn`, so an implementation of it has to spell the same attribute; the
# alternative is hand-writing a `Pin<Box<dyn Future>>` signature with two elided lifetimes in it.
# Already in the tree underneath `hickory-server` at this version, so this is a new edge rather than
# a new package — the reasoning `windows-sys` and `futures-util` are chosen by. **The only
# `async_trait` in this workspace**: every trait MixEngine declares itself is either synchronous or
# uses a native `async fn`, and this one belongs to somebody else.
async-trait = "0.1.92"
# **`keyring`'s own Linux backend, taken as a direct edge on purpose** — the one exception to the
# one-crate-per-concern table in ../standards/rust.md, argued in ADR 0013. `keyring` reports a
# machine with no secret service as a generic platform failure, and the only thing that tells that
# apart from a store which is present and refusing is the D-Bus error *name* riding inside the boxed
# source. The name is in the D-Bus specification, so every bus implementation must use it; the
# message beside it is the implementation's own wording and already differs between distributions.
# Already in the tree underneath `keyring` at this version, so this is a new edge rather than a new
# package — and CI keeps it that way, because two copies would make the downcast answer `None`
# forever without failing to compile.
dbus-secret-service = { version = "4.1.0", default-features = false }
directories = "6.0.0"
# One `stream::unfold`, for the Server-Sent Events body. Already present through `sqlx`, so this is
# a new edge rather than a new package.
futures-util = { version = "0.3.33", default-features = false }
# Gzip, for the `.tar.gz` half of what the packaging pipeline produces: it packs `.tar.zst` and
# falls back to `.tar.gz` when the runner's tar has no zstd, so both shapes are published and a
# client that reads only one would fail on whichever cell fell back that day. Default features are
# the pure-Rust `miniz_oxide` backend; every `zlib*` feature is a C library this does not need.
flate2 = "1.1.9"
# The one source of randomness *this workspace's own code* calls, and it is here because entropy is
# the operating system's: `getrandom` is `BCryptGenRandom` on Windows, `getentropy` on macOS and
# `getrandom(2)` on Linux, which is exactly the shape every other capability in `mixengine-platform`
# has. Already in the tree at this major through `rand` 0.10 — which is `hickory`, for DNS message
# ids — and through `tempfile`, so this is a new edge rather than a new package. It is also the whole
# of the dependency: no distributions, no PRNG, no seeding, because a password is a handful of bytes
# read once and never a stream.
#
# The tree does carry a second copy, `getrandom` 0.2, under `ring` and `minisign`. It is not this
# edge that puts it there and no choice here removes it; it is written down in ../deny.toml with the
# rest of the duplicates that check accepts.
getrandom = "0.4.3"
# The DNS server of ../standards/rust.md's table, and only the server half: `hickory-resolver` is
# deliberately absent, because this daemon answers for the TLDs it manages and REFUSES everything
# else rather than forwarding — the T44 design, D1. `hickory-server` has **no default features**, so
# what is taken here is the bare UDP/TCP server: no `resolver`, no `recursor`, no `__tls`/`__quic`,
# no `__dnssec`, and no `sqlite`, which would put a second SQLite in a tree that already has one
# through `sqlx`.
hickory-server = { version = "0.26.1", default-features = false }
# The message types the server hands over and the answers it writes back. A direct edge rather than
# a reach through `hickory-server`'s re-export, because the whole of the answering policy is written
# against these types and the tests build messages without a server at all — the T44 design, D5.
hickory-proto = { version = "0.26.1", default-features = false, features = ["std"] }
# The JSON-RPC transport, per ../standards/rust.md. Features are chosen per crate: the daemon takes
# `server`, its tests take `client`.
http-body-util = "0.1.4"
hyper = { version = "1.11.0", default-features = false }
hyper-util = { version = "0.1.20", default-features = false }
# Which network interfaces this machine has, for LAN sharing (T74). One crate rather than three
# per-OS modules: `getifaddrs` and `GetAdaptersAddresses` already sit behind it, and the filter that
# turns its answer into a shareable interface is the same on all three systems.
#
# **Shared with `mdns-sd` deliberately** — roadmap task T75. That crate depends on this one, and a
# version this workspace pinned lower produced two `if-addrs` in the lock file, which `cargo deny`
# refuses as a duplicate. The two have to move together.
if-addrs = "0.15.0"
# The OS credential store, per ../standards/rust.md, and the only way a password reaches a managed
# process at all (ADR 0006). `default-features = false` is not a trim here but a requirement: with no
# feature selected this crate compiles a backend that stores nothing and answers every read with
# "no entry", so each OS names its own in `mixengine-platform`'s manifest and a missing one is a
# build that fails rather than a keyring that silently forgets.
keyring = { version = "3.6.3", default-features = false }
# Unix only, and only in `mixengine-platform`: `getuid` for the peer check, and `sockaddr_un` for
# the one length limit that differs between Linux and macOS.
libc = "0.2.189"
# The mDNS responder of T75, and the responder half alone: a shared site is *advertised* here and
# nothing in this workspace browses for services. Pure Rust and cross-platform, so it lives in the
# daemon beside `hickory-server` rather than behind a platform trait -- it makes no OS-specific call,
# and the platform rule exists to keep `#[cfg]` out of logic rather than to route every socket
# through a trait.
#
# **Coexisting with the responder a desktop already runs is measured, not assumed.** On Windows on
# 2026-08-31 `svchost` (dnscache) and `msedge` both held UDP 5353 before this bound alongside them,
# announced, and answered a query. macOS's `mDNSResponder` and Linux's `avahi-daemon` are what CI
# answers for; a bind that fails is a share by address, never a share that failed.
mdns-sd = { version = "0.21.1", default-features = false }
# The template engine every generated config file is rendered by, per ../standards/rust.md. Jinja
# rather than a Rust-syntax templating crate because what these templates are is *other languages* —
# a Caddyfile, a `my.cnf`, an `nginx.conf` — and a `{{ }}` that no host language claims is what keeps
# a template readable as the file it produces.
#
# `default-features = false` drops the halves this has no use for: `loader` and `multi_template`
# (templates are compiled into the binary, one per file, and never looked up by name at run time),
# `macros` and `deserialization`, `debug` (its extra failure context is written for somebody editing
# a template, and the only people who edit these are us, with a compile away), and `key_interning`,
# whose thread-local cache pays off across thousands of renders rather than the handful a service
# start costs. What is kept is `builtins` — `default`, `join`, `lower` and the rest, which a template
# without would push into the Rust that calls it — and `serde`, which is how a context reaches a
# template at all.
minijinja = { version = "2.24.0", default-features = false, features = [
    "builtins",
    "serde",
    "std_collections",
] }
# Verifying the package index, and nothing else. By minisign's own author, **zero dependencies**, and
# it handles the pre-hashed form the publishing pipeline actually produces — a hand-rolled
# `ed25519` check over the file bytes would fail every real signature, and would be "fixed" by
# turning the check off. `minisign` (the signing half) is a dev-dependency of `mixengine-testkit`
# and must never appear here: nothing shipped has any business holding a signing key.
minisign-verify = "0.2.5"
# `ReadyCheck::LogPattern`, whose regex `mixengine-proto` deliberately carries as a string: a spec is
# data, and the engine belongs to whoever runs the check. The Unicode features are not a nicety here:
# a feature this crate is missing is not a pattern that behaves differently, it is a pattern that
# **fails to compile at all**, so trimming one turns a spec somebody wrote into a service that never
# becomes ready. `unicode-case` earns its place first — `(?i)ready for connections` is the pattern a
# spec author reaches for before any other, and without it the regex engine refuses the `i` flag
# outright. `unicode-perl` is `\w`, `\d` and `\s`; `unicode-gencat` is `\p{L}` and `\p{N}`. What is
# left out is script, segmentation and case-*folding* tables, which no log line is matched with.
#
# `crates/mixengine-supervisor/src/ready.rs` asserts this set compiles the syntax a spec may use, so
# a future trim is a test that fails rather than a service that hangs for its whole timeout.
# The only outbound HTTP in this workspace: fetching the package index and, from T21, the artifacts
# it names. `hyper` serves the local IPC socket and answers loopback health checks; neither wants a
# TLS stack, and `mixengine-supervisor` says so where it refuses an `https://` check.
#
# The certificate generator of ../standards/rust.md's table, and the whole of what lets an internal
# CA exist without shelling out to `openssl`. **Default features plus `x509-parser`**: the default
# backend is `ring`, and `ring` is here for this edge and no other — `rustls` in this tree runs on
# `aws-lc-rs`, argued below, so `rcgen` is what puts `ring` in. That is a new package and not merely
# a new edge, taken knowingly: `rcgen`'s `aws_lc_rs` feature would unify the two backends, but it
# also enables `aws-lc-rs/aws-lc-sys`, and which provider signs a certificate is a decision this file
# is not the place to change. `x509-parser` unlocks `Issuer::from_ca_cert_pem`, which is how a
# *stored* CA signs a leaf (T50), and brings the parser T48 already needs — at the version rcgen
# pins, so the tree carries one copy and not two.
rcgen = { version = "0.14.9", features = ["x509-parser"] }
# The TLS client `mix cert status` uses to ask this home's own front end what it is serving —
# roadmap task T53, and the first thing in this workspace that measures a padlock rather than
# inferring one from a file.
#
# **A new edge and not a new package.** `cargo tree -p mixengine-daemon -i aws-lc-rs` already shows
# `rustls` reaching the daemon through `mixengine-core` -> `reqwest`, and `tokio-rustls` with it, so
# nothing new is downloaded, audited or shipped. Each is pinned at the version already resolved, so
# the tree carries one copy and not two — which is the condition that makes "a new edge" true rather
# than merely stated.
#
# **No `ring` feature, deliberately.** The provider in this tree is `aws-lc-rs`; enabling a second
# one leaves `rustls` with no process-level default and `ClientConfig::builder()` panics at run time
# inside a running daemon. Every config built here names its provider explicitly for that reason, so
# a tree that switched providers fails to compile instead of failing in the field.
rustls = "0.23.43"
tokio-rustls = "0.26.4"
# The PEM codec `rcgen` itself uses, taken directly at the version it pins rather than as a second
# one: reading a certificate off disk means decoding the same envelope rcgen wrote.
pem = "3.0.2"

# Rendering the handbook to HTML — roadmap task T90. A **dev**-dependency of `mixengine-docs` and of
# nothing else: the site generator is an example in that crate, so no Markdown renderer is linked
# into `mix`, which prints the Markdown itself. Raw-HTML passthrough is switched off where it is
# used, which is how "the site carries no JavaScript" is held by the generator rather than by a
# reviewer.
#
# `default-features = false` drops `getopts`, which is there for the crate's own example binary;
# `html` is the renderer itself and is the whole reason for the edge.
pulldown-cmark = { version = "0.13.0", default-features = false, features = ["html"] }

# The QR code `mix site share` prints — T74. `default-features = false` drops the `image` and `svg`
# renderers with everything under them: what this needs is the matrix and the two-block-per-row
# rendering it already has, into a terminal. A phone camera is the only reader, and it reads the
# same matrix whichever renderer drew it.
qrcode = { version = "0.14.1", default-features = false }
# Default features are kept rather than trimmed, which is unusual in this file and deliberate. The
# `rustls` default enables `rustls-platform-verifier`, so certificates are judged by the operating
# system's own verifier: MixEngine then works behind a corporate proxy that installs its own root,
# and `MIXENGINE_MIRROR_URL` can name a mirror with an internal certificate. A bundled root store
# would refuse both with nothing the user could do. That is affordable because TLS is not what
# decides whether an index is ours — the Ed25519 signature is, and it is checked after the bytes
# arrive however they arrived.
reqwest = "0.13.4"
regex = { version = "1.13.1", default-features = false, features = [
    "perf",
    "std",
    "unicode-case",
    "unicode-gencat",
    "unicode-perl",
] }
# Zstandard, **decoding only**. The publishing pipeline packs `.tar.zst`, so a client that could not
# read one could install nothing on macOS or Linux. Pure Rust and no build script, which the C
# `zstd` binding is not — and this tree already cross-compiles to `aarch64-pc-windows-msvc`, where
# every avoidable C dependency is one fewer thing to get a toolchain for. Its encoding half is not
# missing but absent by design: nothing shipped writes a zstd stream, and the fixtures that must
# write one use the C `zstd` crate in `mixengine-testkit` instead. That is the same split
# `minisign-verify`/`minisign` uses one layer up, with the same benefit — what the product reads is
# proved against what a *different* implementation wrote.
ruzstd = "0.9.0"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
# The hash every downloaded artifact is checked against, pinned in the signed index
# (../architecture/security-model.md). RustCrypto's, which is what the rest of the ecosystem audits.
# **0.10 rather than the current 0.11**, deliberately: `sqlx` depends on 0.10, both crates that
# want a hash already depend on `sqlx`, and taking the newer one would put two copies of `sha2`,
# `digest` and their `generic-array`/`typenum` chain in the tree for an API this uses four calls of.
# `deny.toml`'s multiple-versions check exists to notice exactly that. Move when sqlx does.
sha2 = "0.10.9"
# Only the pieces SQLite needs. `default-features = false` drops `any` (a runtime-dispatched driver
# for databases we do not speak) and `json` (a column type we do not use — JSON is stored as TEXT
# and parsed by `serde_json`, which is already here). `sqlite-bundled` rather than `sqlite`: the
# umbrella feature also switches on `load-extension`, and a database that can `SELECT
# load_extension('…')` is arbitrary code execution reachable from a data file. `tls-none` is stated
# rather than omitted because sqlx picks a TLS stack when asked for one, and a local file has no
# transport to secure.
sqlx = { version = "0.9.0", default-features = false, features = [
    "runtime-tokio",
    "sqlite-bundled",
    "migrate",
    "macros",
    "tls-none",
] }
# Per-process CPU and RSS — the reading T71's sampler takes, and the crate `.claude/standards/rust.md`
# already names for "process/system info". `default-features = false` with `system` alone: this
# workspace never asks about disks, networks, components or logged-in users, and every one of those
# is a syscall surface compiled in for nothing. `multithread` is deliberately left off — a refresh
# that spawns a thread pool to read a few dozen processes costs more than it saves, and this one runs
# on a timer for as long as the daemon does.
sysinfo = { version = "0.39.6", default-features = false, features = ["system"] }
# The container inside every `.tar.zst` and `.tar.gz`. Unpacking is where an archive gets to choose
# where its bytes land, so the crate that resolves an entry's path matters more than its size:
# `Entry::unpack_in` refuses to write outside the directory it is given, and this module checks the
# entry path itself before calling it so the refusal is ours and has a test. It is also what keeps
# the mode bits and the symlinks — both of which are the operating system's business and therefore
# belong to a dependency rather than to a `#[cfg]` in `mixengine-core`.
tar = "0.4.46"
thiserror = "2.0.20"
tokio = "1.53.1"
# `CancellationToken`, which ../standards/rust.md names as the shutdown path every spawned task
# hangs off. Written by the tokio project and versioned with it; the `sync` module it lives in needs
# no feature, so `default-features = false` leaves the codec, io and net helpers out of the build.
tokio-util = { version = "0.7.18", default-features = false }
toml = "1.1.4"
# Writing `mixengine.toml` back (T39). `toml` cannot: a `Value` carries no formatting, so
# serialising a fresh document over the user's file would take their comments, their key order and
# the `[site]` block they wrote by hand with it — to the one file whose entire purpose is to be
# read by a person. Already in `Cargo.lock` at this version underneath `toml` itself, so this is a
# new *direct* edge rather than a new subtree.
toml_edit = "0.25.13"
tracing = "0.1.44"
tracing-subscriber = "0.3.23"
# The published API contract — roadmap task T56, and the crate ../standards/rust.md's table already
# names for TS bindings. **Optional, and off by default**: `mixengine-proto` is in every binary this
# project ships, `mixengine-elevate` included, and its manifest's first sentence is that it is
# serde-only. Nothing a user runs compiles this, which the elevated helper's dependency budget in
# `.github/elevate-dependencies.txt` keeps true rather than merely stated.
#
# `serde-compat` is the default and the whole reason it works — the wire shape is written in
# `#[serde(…)]` attributes and nowhere else. `serde-json-impl` is `rpc::Request::params` and the four
# other `serde_json::Value` positions, which would otherwise be `unknown` and a cast at every client
# call site. `no-serde-warnings` silences one compiler note per `serde(transparent)` and
# `serde(deny_unknown_fields)` in that crate — thirty of them, on every `--all-features` compile, for
# two attributes with no TypeScript meaning; measured not to be lint warnings, so this is signal
# hygiene rather than a build fix, and deny.toml's own reasoning one level up ("a warning in a CI log
# is a warning nobody opens") is why it is worth spending a feature on. `format` is deliberately
# absent: it is a `dprint` subtree for whitespace this generator already gets right.
#
# **Three new packages and not merely three new edges** — `ts-rs`, `ts-rs-macros` and `termcolor`,
# which the macro crate uses to colour the diagnostics `no-serde-warnings` turns off and which is not
# optional upstream. All three are host-only and reachable from nothing that builds a binary, which
# is the whole reason the feature is off by default; `winapi-util` under `termcolor` resolves to the
# `windows-sys` already in this tree, so the ban on `winapi` is untouched.
ts-rs = { version = "12.0.1", features = ["serde-json-impl", "no-serde-warnings"] }
# `windows-sys` rather than `windows`, which ../standards/rust.md names: both are Microsoft's and
# generated from the same metadata, but `windows-sys` is already in this tree — tokio, mio and
# socket2 all depend on it — so the named-pipe security work costs no new dependency and no second
# copy of the same bindings, which `deny.toml`'s multiple-versions check would report. The ban that
# matters is on `winapi`, and it stays.
windows-sys = "0.61.2"
# The Windows half of what the pipeline publishes. `default-features = false` with `deflate-flate2`
# alone is not a trim for its own sake: the defaults compile bzip2, lzma, xz, ppmd, deflate64, AES
# and a second zstd, which are six more decompressors reachable from a downloaded file than any
# artifact this project publishes uses — `php_windows.py` packs with `ZIP_DEFLATED` and nothing
# else. The backend is the `flate2` already above rather than a second copy of one.
# Reading a certificate back off disk rather than trusting what was written. The validity window
# and the subject in `cert.ca_status` are what the *file* says: a home restored from a backup, or
# one whose certificate was rotated by hand, is exactly the case where a remembered value and the
# bytes disagree. Version pinned to the one `rcgen` resolves, so there is one copy of it.
x509-parser = "0.18.1"
zip = { version = "8.6.0", default-features = false, features = ["deflate-flate2"] }

cargo_metadata = "0.23.1"
tempfile = "3.24.0"
# The encoding half of `ruzstd`, and a **dev-dependency of `mixengine-testkit` only** — see the note
# on `ruzstd` for why the two are deliberately different implementations. This one binds the
# reference C library, which is affordable here and would not be in a shipped binary.
zstd = "0.13.3"

# Lints apply to every member through `[lints] workspace = true`.
[workspace.lints.rust]
missing_debug_implementations = "warn"
rust_2018_idioms = { level = "warn", priority = -1 }
unreachable_pub = "warn"
unsafe_code = "deny"

# Intra-doc links are resolved by rustdoc and by nothing else, so a link to an item that does not
# exist is invisible to `clippy` and to `cargo test --doc` alike. Denied here rather than only
# through `RUSTDOCFLAGS` in CI so that a plain `cargo doc` fails on a developer machine too — CI
# sets the variable as well, which additionally covers the rustc warnings rustdoc's compile pass
# raises.
[workspace.lints.rustdoc]
all = { level = "deny", priority = -1 }

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
unwrap_used = "warn"

[profile.release]
codegen-units = 1
lto = "thin"
# Debug info goes, symbol names stay: a daemon crash report is worthless without function names,
# and "copy diagnostics" (T66) is meant to produce something a human can act on.
strip = "debuginfo"
# `panic = "abort"` must never be set: the RPC layer catches a panic per request and returns
# `internal` instead of taking every managed service down with it.
