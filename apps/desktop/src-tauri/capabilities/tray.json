{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray",
  "description": "Capability for the tray panel (T168): the daemon's commands, opening a site in the browser, and logging - nothing that reads or writes the user's files",
  "windows": ["tray"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "opener:allow-default-urls",
    "log:default"
  ]
}
