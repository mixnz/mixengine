{
  "name": "@mixlab/sync-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MixLab's sync server on Cloudflare Workers — the default instance.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "~5.8.3",
    "wrangler": "^4.0.0"
  }
}
